<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" version="2.0">
  <channel>
    <title>GameMaps L4D2 Files</title>
    <link>https://www.gamemaps.com/l4d2/files</link>
    <description>RSS feed for GameMaps L4D2 Files.</description>
    <language>en-US</language>
    <generator>Multi-RSS</generator>
    <lastBuildDate>Mon, 11 May 2026 00:47:51 +0000</lastBuildDate>
    <item>
      <title>[ Mod ] George A. Romero DEAD MODE (works MP)</title>
      <link>https://www.gamemaps.com/details/24818</link>
      <guid>https://www.gamemaps.com/details/24818</guid>
      <description><![CDATA[
    
    <style>
        .gmLightboxOpen {
            overflow: hidden;
        }

        .gm-actions {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
            margin: 0 0 12px 0;
        }

        .gm-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-height: 38px;
            padding: 8px 14px;
            border-radius: 8px;
            text-decoration: none;
            color: #ffffff;
            font-weight: 600;
            font-size: 14px;
        }

        .gm-btn-download {
            background: #1d9f61;
        }

        .gm-btn-steam {
            background: #1d5fb5;
        }

        .gm-media-label {
            margin: 6px 0 8px 0;
            font-size: 14px;
            font-weight: 600;
            letter-spacing: .2px;
        }

        .cleanSpoilerGallery {
            margin: 14px 0;
            border-radius: 10px;
            overflow: hidden;
        }

        .cleanSpoilerGallery .ipsSpoiler_header {
            padding: 11px 14px;
            background: linear-gradient(180deg, rgba(255,255,255,.07), rgba(255,255,255,.03));
            border: 1px solid rgba(255,255,255,.08);
            border-bottom: 0;
            border-radius: 10px 10px 0 0;
        }

        .cleanSpoilerGallery .ipsSpoiler_header span {
            font-size: 13px;
            font-weight: 600;
            letter-spacing: .2px;
            color: #e8e8e8;
        }

        .cleanSpoilerGallery .ipsSpoiler_contents {
            padding: 12px;
            background: rgba(255,255,255,.035);
            border: 1px solid rgba(255,255,255,.08);
            border-radius: 0 0 10px 10px;
        }

        .cleanSpoilerGallery .spoilerGalleryGrid {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            align-items: flex-start;
        }

        .cleanSpoilerGallery .spoilerGalleryItem {
            display: inline-block;
            overflow: hidden;
            border-radius: 8px;
            background: #111;
            border: 1px solid rgba(255,255,255,.07);
            width: 188px;
            text-decoration: none;
        }

        .cleanSpoilerGallery .spoilerGalleryItem img {
            width: 100%;
            height: 112px;
            object-fit: cover;
            display: block;
            border-radius: 8px;
            transition: transform .25s ease, filter .25s ease;
        }

        .cleanSpoilerGallery .spoilerGalleryItem:hover img {
            transform: scale(1.025);
            filter: brightness(1.06);
        }

        .gm-description-text {
            white-space: pre-wrap;
            line-height: 1.6;
            font-size: 15px;
        }

        .gm-description-text a {
            color: #1d5fb5;
            text-decoration: underline;
        }

        .gm-description-text a.gm-inline-steam {
            display: inline-block;
            margin-top: 6px;
            padding: 3px 8px;
            border-radius: 6px;
            border: 1px solid #1d5fb5;
            text-decoration: none;
        }

        .gmLightbox {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, .86);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 2147483640;
            padding: 20px;
        }

        .gmLightbox.is-open {
            display: flex;
        }

        .gmLightboxImage {
            max-width: min(92vw, 1500px);
            max-height: 84vh;
            border-radius: 8px;
            object-fit: contain;
            box-shadow: 0 12px 36px rgba(0, 0, 0, .5);
        }

        .gmLightboxBtn {
            position: fixed;
            border: 0;
            background: rgba(22, 22, 22, .75);
            color: #fff;
            cursor: pointer;
            border-radius: 8px;
            font-size: 22px;
            line-height: 1;
            padding: 10px 12px;
        }

        .gmLightboxClose {
            top: 16px;
            right: 16px;
            font-size: 28px;
            padding: 8px 12px;
        }

        .gmLightboxPrev {
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxNext {
            right: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxCounter {
            position: fixed;
            bottom: 16px;
            left: 50%;
            transform: translateX(-50%);
            color: #fff;
            background: rgba(18, 18, 18, .72);
            border-radius: 999px;
            padding: 6px 12px;
            font-size: 12px;
        }

        @media (max-width: 640px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: calc(50% - 5px);
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 105px;
            }

            .gm-btn {
                width: 100%;
            }

            .gmLightboxPrev,
            .gmLightboxNext {
                font-size: 18px;
                padding: 8px 10px;
            }
        }

        @media (max-width: 420px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: 100%;
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 150px;
            }
        }
    </style>
    
    <div class="gm-actions"><a class="gm-btn gm-btn-download" href="https://www.gamemaps.com/download/eyJpZHMiOlsyNDgxOF19?s=a98302d75265544c10cef502561b32b4943ac4bbad150af178bc1d6b01d2d10a&amp;e=1778471270" target="_blank" rel="nofollow noopener noreferrer">Direct Download</a></div>
    
    <div class="gm-media-label">Media</div>
    <div class="ipsSpoiler cleanSpoilerGallery" data-ipsspoiler>
        <div class="ipsSpoiler_header">
            <span>Media gallery</span>
        </div>
        <div class="ipsSpoiler_contents ipsClearfix">
            <div class="spoilerGalleryGrid">
                
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/george_a__romero_dead_mode__works_mp__24818_0.jpg?1612770514"
               data-gm-gallery="gm-gallery-24818"
               data-gm-index="0"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-24818"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/george_a__romero_dead_mode__works_mp__24818_0.jpg?1612770514" alt="GameMaps media 1" loading="lazy">
            </a>
            
            </div>
        </div>
    </div>

    <div class="gmLightbox" data-gm-lightbox="gm-gallery-24818" hidden>
        <button type="button" class="gmLightboxBtn gmLightboxClose" aria-label="Close">&times;</button>
        <button type="button" class="gmLightboxBtn gmLightboxPrev" aria-label="Previous image">&#10094;</button>
        <img class="gmLightboxImage" src="" alt="">
        <button type="button" class="gmLightboxBtn gmLightboxNext" aria-label="Next image">&#10095;</button>
        <div class="gmLightboxCounter">1 / 1</div>
    </div>

    
    <script>
    (function () {
        if (window.__gmGalleryInitialized) {
            return;
        }
        window.__gmGalleryInitialized = true;

        function getOpenModal() {
            return document.querySelector('.gmLightbox.is-open');
        }

        function getGalleryLinks(group) {
            var allLinks = Array.prototype.slice.call(document.querySelectorAll('a[data-gm-gallery]'));
            return allLinks.filter(function (link) {
                return link.getAttribute('data-gm-gallery') === group;
            });
        }

        function closeModal(modal) {
            if (!modal) {
                return;
            }
            modal.classList.remove('is-open');
            modal.setAttribute('hidden', 'hidden');
            var image = modal.querySelector('.gmLightboxImage');
            if (image) {
                image.removeAttribute('src');
            }
            document.documentElement.classList.remove('gmLightboxOpen');
        }

        function renderModal(modal) {
            var links = modal.__gmLinks || [];
            if (!links.length) {
                return;
            }

            var index = modal.__gmIndex || 0;
            if (index < 0) {
                index = links.length - 1;
            }
            if (index >= links.length) {
                index = 0;
            }

            modal.__gmIndex = index;
            var activeLink = links[index];
            var image = modal.querySelector('.gmLightboxImage');
            var thumb = activeLink.querySelector('img');
            var counter = modal.querySelector('.gmLightboxCounter');

            if (image) {
                image.setAttribute('src', activeLink.getAttribute('href'));
                image.setAttribute('alt', thumb ? (thumb.getAttribute('alt') || '') : '');
            }

            if (counter) {
                counter.textContent = (index + 1) + ' / ' + links.length;
            }
        }

        function openModal(modal, links, startIndex) {
            if (!modal || !links.length) {
                return;
            }
            modal.__gmLinks = links;
            modal.__gmIndex = startIndex;
            renderModal(modal);
            modal.removeAttribute('hidden');
            modal.classList.add('is-open');
            document.documentElement.classList.add('gmLightboxOpen');
        }

        function stepModal(modal, delta) {
            if (!modal) {
                return;
            }
            modal.__gmIndex = (modal.__gmIndex || 0) + delta;
            renderModal(modal);
        }

        document.addEventListener('click', function (event) {
            var galleryItem = event.target.closest('a[data-gm-gallery]');
            if (galleryItem) {
                var group = galleryItem.getAttribute('data-gm-gallery');
                var modal = document.querySelector('.gmLightbox[data-gm-lightbox="' + group + '"]');
                var links = getGalleryLinks(group);
                var startIndex = parseInt(galleryItem.getAttribute('data-gm-index') || '0', 10);
                if (modal && links.length) {
                    event.preventDefault();
                    openModal(modal, links, startIndex);
                }
                return;
            }

            var closeButton = event.target.closest('.gmLightboxClose');
            if (closeButton) {
                closeModal(closeButton.closest('.gmLightbox'));
                return;
            }

            var prevButton = event.target.closest('.gmLightboxPrev');
            if (prevButton) {
                stepModal(prevButton.closest('.gmLightbox'), -1);
                return;
            }

            var nextButton = event.target.closest('.gmLightboxNext');
            if (nextButton) {
                stepModal(nextButton.closest('.gmLightbox'), 1);
                return;
            }

            var openModalElement = event.target.classList.contains('gmLightbox') ? event.target : null;
            if (openModalElement) {
                closeModal(openModalElement);
            }
        });

        document.addEventListener('keydown', function (event) {
            var modal = getOpenModal();
            if (!modal) {
                return;
            }

            if (event.key === 'Escape') {
                closeModal(modal);
            } else if (event.key === 'ArrowLeft') {
                stepModal(modal, -1);
            } else if (event.key === 'ArrowRight') {
                stepModal(modal, 1);
            }
        });
    })();
    </script>
    
    
    <div class="gm-description-text">READ WITH ATTENTION!

ENG:
For the mod to work in online multiplayer, you need to activate it in the addons menu, then START a SINGLE PLAYER game, go back to the menu and you can create your local game to PLAY Multi-player. Its very simple.

PT/BR:
Para o mod funcionar em multplayer online, você precisa ativa-lo no menu de addons, e depois entrar em uma partida SINGLE PLAYER, voltar o menu e poderá criar sua partida local multplayer.

ES:
Para que el mod funcione en multijugador en línea, debes activarlo en el menú de complementos, luego INICIAR un juego de UN SOLO JUGADOR, volver al menú y puedes crear tu juego local.

_
This mod is not designed to play with NPCs

MOD WORKS IN GAME MODES:
CAMPAING/COOP
REALISM
SURVIVAL
VERUS-SURVIVAL
VERSUS
VERSUS REALISM

CAMPAING:
-zombies are slow, like a living-dead
-the dead really only die when they destroy their brains.
-zombies make new noises.
-There are no specials or tanks.
-weapons with different recoils.
-always first person
-pistols need ammo.
-pipe bombs are grenades , there is no vomiting bile.
-more hordes.
-zombies are strongs.
among others...

Versus:
-Specials have no powers, but can deliver stronger punches, like ordinary zombies.
-there is no melee
-is similar to the campaign.</div>
    ]]></description>
      <content:encoded><![CDATA[
    
    <style>
        .gmLightboxOpen {
            overflow: hidden;
        }

        .gm-actions {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
            margin: 0 0 12px 0;
        }

        .gm-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-height: 38px;
            padding: 8px 14px;
            border-radius: 8px;
            text-decoration: none;
            color: #ffffff;
            font-weight: 600;
            font-size: 14px;
        }

        .gm-btn-download {
            background: #1d9f61;
        }

        .gm-btn-steam {
            background: #1d5fb5;
        }

        .gm-media-label {
            margin: 6px 0 8px 0;
            font-size: 14px;
            font-weight: 600;
            letter-spacing: .2px;
        }

        .cleanSpoilerGallery {
            margin: 14px 0;
            border-radius: 10px;
            overflow: hidden;
        }

        .cleanSpoilerGallery .ipsSpoiler_header {
            padding: 11px 14px;
            background: linear-gradient(180deg, rgba(255,255,255,.07), rgba(255,255,255,.03));
            border: 1px solid rgba(255,255,255,.08);
            border-bottom: 0;
            border-radius: 10px 10px 0 0;
        }

        .cleanSpoilerGallery .ipsSpoiler_header span {
            font-size: 13px;
            font-weight: 600;
            letter-spacing: .2px;
            color: #e8e8e8;
        }

        .cleanSpoilerGallery .ipsSpoiler_contents {
            padding: 12px;
            background: rgba(255,255,255,.035);
            border: 1px solid rgba(255,255,255,.08);
            border-radius: 0 0 10px 10px;
        }

        .cleanSpoilerGallery .spoilerGalleryGrid {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            align-items: flex-start;
        }

        .cleanSpoilerGallery .spoilerGalleryItem {
            display: inline-block;
            overflow: hidden;
            border-radius: 8px;
            background: #111;
            border: 1px solid rgba(255,255,255,.07);
            width: 188px;
            text-decoration: none;
        }

        .cleanSpoilerGallery .spoilerGalleryItem img {
            width: 100%;
            height: 112px;
            object-fit: cover;
            display: block;
            border-radius: 8px;
            transition: transform .25s ease, filter .25s ease;
        }

        .cleanSpoilerGallery .spoilerGalleryItem:hover img {
            transform: scale(1.025);
            filter: brightness(1.06);
        }

        .gm-description-text {
            white-space: pre-wrap;
            line-height: 1.6;
            font-size: 15px;
        }

        .gm-description-text a {
            color: #1d5fb5;
            text-decoration: underline;
        }

        .gm-description-text a.gm-inline-steam {
            display: inline-block;
            margin-top: 6px;
            padding: 3px 8px;
            border-radius: 6px;
            border: 1px solid #1d5fb5;
            text-decoration: none;
        }

        .gmLightbox {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, .86);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 2147483640;
            padding: 20px;
        }

        .gmLightbox.is-open {
            display: flex;
        }

        .gmLightboxImage {
            max-width: min(92vw, 1500px);
            max-height: 84vh;
            border-radius: 8px;
            object-fit: contain;
            box-shadow: 0 12px 36px rgba(0, 0, 0, .5);
        }

        .gmLightboxBtn {
            position: fixed;
            border: 0;
            background: rgba(22, 22, 22, .75);
            color: #fff;
            cursor: pointer;
            border-radius: 8px;
            font-size: 22px;
            line-height: 1;
            padding: 10px 12px;
        }

        .gmLightboxClose {
            top: 16px;
            right: 16px;
            font-size: 28px;
            padding: 8px 12px;
        }

        .gmLightboxPrev {
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxNext {
            right: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxCounter {
            position: fixed;
            bottom: 16px;
            left: 50%;
            transform: translateX(-50%);
            color: #fff;
            background: rgba(18, 18, 18, .72);
            border-radius: 999px;
            padding: 6px 12px;
            font-size: 12px;
        }

        @media (max-width: 640px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: calc(50% - 5px);
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 105px;
            }

            .gm-btn {
                width: 100%;
            }

            .gmLightboxPrev,
            .gmLightboxNext {
                font-size: 18px;
                padding: 8px 10px;
            }
        }

        @media (max-width: 420px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: 100%;
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 150px;
            }
        }
    </style>
    
    <div class="gm-actions"><a class="gm-btn gm-btn-download" href="https://www.gamemaps.com/download/eyJpZHMiOlsyNDgxOF19?s=a98302d75265544c10cef502561b32b4943ac4bbad150af178bc1d6b01d2d10a&amp;e=1778471270" target="_blank" rel="nofollow noopener noreferrer">Direct Download</a></div>
    
    <div class="gm-media-label">Media</div>
    <div class="ipsSpoiler cleanSpoilerGallery" data-ipsspoiler>
        <div class="ipsSpoiler_header">
            <span>Media gallery</span>
        </div>
        <div class="ipsSpoiler_contents ipsClearfix">
            <div class="spoilerGalleryGrid">
                
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/george_a__romero_dead_mode__works_mp__24818_0.jpg?1612770514"
               data-gm-gallery="gm-gallery-24818"
               data-gm-index="0"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-24818"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/george_a__romero_dead_mode__works_mp__24818_0.jpg?1612770514" alt="GameMaps media 1" loading="lazy">
            </a>
            
            </div>
        </div>
    </div>

    <div class="gmLightbox" data-gm-lightbox="gm-gallery-24818" hidden>
        <button type="button" class="gmLightboxBtn gmLightboxClose" aria-label="Close">&times;</button>
        <button type="button" class="gmLightboxBtn gmLightboxPrev" aria-label="Previous image">&#10094;</button>
        <img class="gmLightboxImage" src="" alt="">
        <button type="button" class="gmLightboxBtn gmLightboxNext" aria-label="Next image">&#10095;</button>
        <div class="gmLightboxCounter">1 / 1</div>
    </div>

    
    <script>
    (function () {
        if (window.__gmGalleryInitialized) {
            return;
        }
        window.__gmGalleryInitialized = true;

        function getOpenModal() {
            return document.querySelector('.gmLightbox.is-open');
        }

        function getGalleryLinks(group) {
            var allLinks = Array.prototype.slice.call(document.querySelectorAll('a[data-gm-gallery]'));
            return allLinks.filter(function (link) {
                return link.getAttribute('data-gm-gallery') === group;
            });
        }

        function closeModal(modal) {
            if (!modal) {
                return;
            }
            modal.classList.remove('is-open');
            modal.setAttribute('hidden', 'hidden');
            var image = modal.querySelector('.gmLightboxImage');
            if (image) {
                image.removeAttribute('src');
            }
            document.documentElement.classList.remove('gmLightboxOpen');
        }

        function renderModal(modal) {
            var links = modal.__gmLinks || [];
            if (!links.length) {
                return;
            }

            var index = modal.__gmIndex || 0;
            if (index < 0) {
                index = links.length - 1;
            }
            if (index >= links.length) {
                index = 0;
            }

            modal.__gmIndex = index;
            var activeLink = links[index];
            var image = modal.querySelector('.gmLightboxImage');
            var thumb = activeLink.querySelector('img');
            var counter = modal.querySelector('.gmLightboxCounter');

            if (image) {
                image.setAttribute('src', activeLink.getAttribute('href'));
                image.setAttribute('alt', thumb ? (thumb.getAttribute('alt') || '') : '');
            }

            if (counter) {
                counter.textContent = (index + 1) + ' / ' + links.length;
            }
        }

        function openModal(modal, links, startIndex) {
            if (!modal || !links.length) {
                return;
            }
            modal.__gmLinks = links;
            modal.__gmIndex = startIndex;
            renderModal(modal);
            modal.removeAttribute('hidden');
            modal.classList.add('is-open');
            document.documentElement.classList.add('gmLightboxOpen');
        }

        function stepModal(modal, delta) {
            if (!modal) {
                return;
            }
            modal.__gmIndex = (modal.__gmIndex || 0) + delta;
            renderModal(modal);
        }

        document.addEventListener('click', function (event) {
            var galleryItem = event.target.closest('a[data-gm-gallery]');
            if (galleryItem) {
                var group = galleryItem.getAttribute('data-gm-gallery');
                var modal = document.querySelector('.gmLightbox[data-gm-lightbox="' + group + '"]');
                var links = getGalleryLinks(group);
                var startIndex = parseInt(galleryItem.getAttribute('data-gm-index') || '0', 10);
                if (modal && links.length) {
                    event.preventDefault();
                    openModal(modal, links, startIndex);
                }
                return;
            }

            var closeButton = event.target.closest('.gmLightboxClose');
            if (closeButton) {
                closeModal(closeButton.closest('.gmLightbox'));
                return;
            }

            var prevButton = event.target.closest('.gmLightboxPrev');
            if (prevButton) {
                stepModal(prevButton.closest('.gmLightbox'), -1);
                return;
            }

            var nextButton = event.target.closest('.gmLightboxNext');
            if (nextButton) {
                stepModal(nextButton.closest('.gmLightbox'), 1);
                return;
            }

            var openModalElement = event.target.classList.contains('gmLightbox') ? event.target : null;
            if (openModalElement) {
                closeModal(openModalElement);
            }
        });

        document.addEventListener('keydown', function (event) {
            var modal = getOpenModal();
            if (!modal) {
                return;
            }

            if (event.key === 'Escape') {
                closeModal(modal);
            } else if (event.key === 'ArrowLeft') {
                stepModal(modal, -1);
            } else if (event.key === 'ArrowRight') {
                stepModal(modal, 1);
            }
        });
    })();
    </script>
    
    
    <div class="gm-description-text">READ WITH ATTENTION!

ENG:
For the mod to work in online multiplayer, you need to activate it in the addons menu, then START a SINGLE PLAYER game, go back to the menu and you can create your local game to PLAY Multi-player. Its very simple.

PT/BR:
Para o mod funcionar em multplayer online, você precisa ativa-lo no menu de addons, e depois entrar em uma partida SINGLE PLAYER, voltar o menu e poderá criar sua partida local multplayer.

ES:
Para que el mod funcione en multijugador en línea, debes activarlo en el menú de complementos, luego INICIAR un juego de UN SOLO JUGADOR, volver al menú y puedes crear tu juego local.

_
This mod is not designed to play with NPCs

MOD WORKS IN GAME MODES:
CAMPAING/COOP
REALISM
SURVIVAL
VERUS-SURVIVAL
VERSUS
VERSUS REALISM

CAMPAING:
-zombies are slow, like a living-dead
-the dead really only die when they destroy their brains.
-zombies make new noises.
-There are no specials or tanks.
-weapons with different recoils.
-always first person
-pistols need ammo.
-pipe bombs are grenades , there is no vomiting bile.
-more hordes.
-zombies are strongs.
among others...

Versus:
-Specials have no powers, but can deliver stronger punches, like ordinary zombies.
-there is no melee
-is similar to the campaign.</div>
    ]]></content:encoded>
      <pubDate>Sat, 09 May 2026 18:09:14 +0000</pubDate>
    </item>
    <item>
      <title>[ 6 Maps ] Final Departure</title>
      <link>https://www.gamemaps.com/details/35601</link>
      <guid>https://www.gamemaps.com/details/35601</guid>
      <description><![CDATA[
    
    <style>
        .gmLightboxOpen {
            overflow: hidden;
        }

        .gm-actions {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
            margin: 0 0 12px 0;
        }

        .gm-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-height: 38px;
            padding: 8px 14px;
            border-radius: 8px;
            text-decoration: none;
            color: #ffffff;
            font-weight: 600;
            font-size: 14px;
        }

        .gm-btn-download {
            background: #1d9f61;
        }

        .gm-btn-steam {
            background: #1d5fb5;
        }

        .gm-media-label {
            margin: 6px 0 8px 0;
            font-size: 14px;
            font-weight: 600;
            letter-spacing: .2px;
        }

        .cleanSpoilerGallery {
            margin: 14px 0;
            border-radius: 10px;
            overflow: hidden;
        }

        .cleanSpoilerGallery .ipsSpoiler_header {
            padding: 11px 14px;
            background: linear-gradient(180deg, rgba(255,255,255,.07), rgba(255,255,255,.03));
            border: 1px solid rgba(255,255,255,.08);
            border-bottom: 0;
            border-radius: 10px 10px 0 0;
        }

        .cleanSpoilerGallery .ipsSpoiler_header span {
            font-size: 13px;
            font-weight: 600;
            letter-spacing: .2px;
            color: #e8e8e8;
        }

        .cleanSpoilerGallery .ipsSpoiler_contents {
            padding: 12px;
            background: rgba(255,255,255,.035);
            border: 1px solid rgba(255,255,255,.08);
            border-radius: 0 0 10px 10px;
        }

        .cleanSpoilerGallery .spoilerGalleryGrid {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            align-items: flex-start;
        }

        .cleanSpoilerGallery .spoilerGalleryItem {
            display: inline-block;
            overflow: hidden;
            border-radius: 8px;
            background: #111;
            border: 1px solid rgba(255,255,255,.07);
            width: 188px;
            text-decoration: none;
        }

        .cleanSpoilerGallery .spoilerGalleryItem img {
            width: 100%;
            height: 112px;
            object-fit: cover;
            display: block;
            border-radius: 8px;
            transition: transform .25s ease, filter .25s ease;
        }

        .cleanSpoilerGallery .spoilerGalleryItem:hover img {
            transform: scale(1.025);
            filter: brightness(1.06);
        }

        .gm-description-text {
            white-space: pre-wrap;
            line-height: 1.6;
            font-size: 15px;
        }

        .gm-description-text a {
            color: #1d5fb5;
            text-decoration: underline;
        }

        .gm-description-text a.gm-inline-steam {
            display: inline-block;
            margin-top: 6px;
            padding: 3px 8px;
            border-radius: 6px;
            border: 1px solid #1d5fb5;
            text-decoration: none;
        }

        .gmLightbox {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, .86);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 2147483640;
            padding: 20px;
        }

        .gmLightbox.is-open {
            display: flex;
        }

        .gmLightboxImage {
            max-width: min(92vw, 1500px);
            max-height: 84vh;
            border-radius: 8px;
            object-fit: contain;
            box-shadow: 0 12px 36px rgba(0, 0, 0, .5);
        }

        .gmLightboxBtn {
            position: fixed;
            border: 0;
            background: rgba(22, 22, 22, .75);
            color: #fff;
            cursor: pointer;
            border-radius: 8px;
            font-size: 22px;
            line-height: 1;
            padding: 10px 12px;
        }

        .gmLightboxClose {
            top: 16px;
            right: 16px;
            font-size: 28px;
            padding: 8px 12px;
        }

        .gmLightboxPrev {
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxNext {
            right: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxCounter {
            position: fixed;
            bottom: 16px;
            left: 50%;
            transform: translateX(-50%);
            color: #fff;
            background: rgba(18, 18, 18, .72);
            border-radius: 999px;
            padding: 6px 12px;
            font-size: 12px;
        }

        @media (max-width: 640px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: calc(50% - 5px);
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 105px;
            }

            .gm-btn {
                width: 100%;
            }

            .gmLightboxPrev,
            .gmLightboxNext {
                font-size: 18px;
                padding: 8px 10px;
            }
        }

        @media (max-width: 420px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: 100%;
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 150px;
            }
        }
    </style>
    
    <div class="gm-actions"><a class="gm-btn gm-btn-download" href="https://www.gamemaps.com/download/eyJpZHMiOlszNTYwMV19?s=96a58604a08b4925e3e0c3216e9b3801152f741f619d32ce6336e3497d57e707&amp;e=1778471271" target="_blank" rel="nofollow noopener noreferrer">Direct Download</a></div>
    
    <div class="gm-media-label">Media</div>
    <div class="ipsSpoiler cleanSpoilerGallery" data-ipsspoiler>
        <div class="ipsSpoiler_header">
            <span>Media gallery</span>
        </div>
        <div class="ipsSpoiler_contents ipsClearfix">
            <div class="spoilerGalleryGrid">
                
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_0.jpg?1776217472"
               data-gm-gallery="gm-gallery-35601"
               data-gm-index="0"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35601"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_0.jpg?1776217472" alt="GameMaps media 1" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_5.jpg?1776279478"
               data-gm-gallery="gm-gallery-35601"
               data-gm-index="1"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35601"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_5.jpg?1776279478" alt="GameMaps media 2" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_1.jpg?1776217477"
               data-gm-gallery="gm-gallery-35601"
               data-gm-index="2"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35601"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_1.jpg?1776217477" alt="GameMaps media 3" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_2.jpg?1776217483"
               data-gm-gallery="gm-gallery-35601"
               data-gm-index="3"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35601"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_2.jpg?1776217483" alt="GameMaps media 4" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_3.jpg?1776217490"
               data-gm-gallery="gm-gallery-35601"
               data-gm-index="4"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35601"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_3.jpg?1776217490" alt="GameMaps media 5" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_4.jpg?1776217499"
               data-gm-gallery="gm-gallery-35601"
               data-gm-index="5"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35601"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_4.jpg?1776217499" alt="GameMaps media 6" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_6.jpg?1776663583"
               data-gm-gallery="gm-gallery-35601"
               data-gm-index="6"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35601"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_6.jpg?1776663583" alt="GameMaps media 7" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_7.jpg?1777393526"
               data-gm-gallery="gm-gallery-35601"
               data-gm-index="7"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35601"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_7.jpg?1777393526" alt="GameMaps media 8" loading="lazy">
            </a>
            
            </div>
        </div>
    </div>

    <div class="gmLightbox" data-gm-lightbox="gm-gallery-35601" hidden>
        <button type="button" class="gmLightboxBtn gmLightboxClose" aria-label="Close">&times;</button>
        <button type="button" class="gmLightboxBtn gmLightboxPrev" aria-label="Previous image">&#10094;</button>
        <img class="gmLightboxImage" src="" alt="">
        <button type="button" class="gmLightboxBtn gmLightboxNext" aria-label="Next image">&#10095;</button>
        <div class="gmLightboxCounter">1 / 8</div>
    </div>

    
    <script>
    (function () {
        if (window.__gmGalleryInitialized) {
            return;
        }
        window.__gmGalleryInitialized = true;

        function getOpenModal() {
            return document.querySelector('.gmLightbox.is-open');
        }

        function getGalleryLinks(group) {
            var allLinks = Array.prototype.slice.call(document.querySelectorAll('a[data-gm-gallery]'));
            return allLinks.filter(function (link) {
                return link.getAttribute('data-gm-gallery') === group;
            });
        }

        function closeModal(modal) {
            if (!modal) {
                return;
            }
            modal.classList.remove('is-open');
            modal.setAttribute('hidden', 'hidden');
            var image = modal.querySelector('.gmLightboxImage');
            if (image) {
                image.removeAttribute('src');
            }
            document.documentElement.classList.remove('gmLightboxOpen');
        }

        function renderModal(modal) {
            var links = modal.__gmLinks || [];
            if (!links.length) {
                return;
            }

            var index = modal.__gmIndex || 0;
            if (index < 0) {
                index = links.length - 1;
            }
            if (index >= links.length) {
                index = 0;
            }

            modal.__gmIndex = index;
            var activeLink = links[index];
            var image = modal.querySelector('.gmLightboxImage');
            var thumb = activeLink.querySelector('img');
            var counter = modal.querySelector('.gmLightboxCounter');

            if (image) {
                image.setAttribute('src', activeLink.getAttribute('href'));
                image.setAttribute('alt', thumb ? (thumb.getAttribute('alt') || '') : '');
            }

            if (counter) {
                counter.textContent = (index + 1) + ' / ' + links.length;
            }
        }

        function openModal(modal, links, startIndex) {
            if (!modal || !links.length) {
                return;
            }
            modal.__gmLinks = links;
            modal.__gmIndex = startIndex;
            renderModal(modal);
            modal.removeAttribute('hidden');
            modal.classList.add('is-open');
            document.documentElement.classList.add('gmLightboxOpen');
        }

        function stepModal(modal, delta) {
            if (!modal) {
                return;
            }
            modal.__gmIndex = (modal.__gmIndex || 0) + delta;
            renderModal(modal);
        }

        document.addEventListener('click', function (event) {
            var galleryItem = event.target.closest('a[data-gm-gallery]');
            if (galleryItem) {
                var group = galleryItem.getAttribute('data-gm-gallery');
                var modal = document.querySelector('.gmLightbox[data-gm-lightbox="' + group + '"]');
                var links = getGalleryLinks(group);
                var startIndex = parseInt(galleryItem.getAttribute('data-gm-index') || '0', 10);
                if (modal && links.length) {
                    event.preventDefault();
                    openModal(modal, links, startIndex);
                }
                return;
            }

            var closeButton = event.target.closest('.gmLightboxClose');
            if (closeButton) {
                closeModal(closeButton.closest('.gmLightbox'));
                return;
            }

            var prevButton = event.target.closest('.gmLightboxPrev');
            if (prevButton) {
                stepModal(prevButton.closest('.gmLightbox'), -1);
                return;
            }

            var nextButton = event.target.closest('.gmLightboxNext');
            if (nextButton) {
                stepModal(nextButton.closest('.gmLightbox'), 1);
                return;
            }

            var openModalElement = event.target.classList.contains('gmLightbox') ? event.target : null;
            if (openModalElement) {
                closeModal(openModalElement);
            }
        });

        document.addEventListener('keydown', function (event) {
            var modal = getOpenModal();
            if (!modal) {
                return;
            }

            if (event.key === 'Escape') {
                closeModal(modal);
            } else if (event.key === 'ArrowLeft') {
                stepModal(modal, -1);
            } else if (event.key === 'ArrowRight') {
                stepModal(modal, 1);
            }
        });
    })();
    </script>
    
    
    <div class="gm-description-text">Left Behind II - Final Departure

&quot;Who will survive?&quot;

The survivors begin they journey from &#x27;
THE STATION
&#x27; and &#x27;
MILITARY BASE
&#x27; they must survive &#x27;
THE FOREST
&#x27; through &#x27;
THE TERRAIN
&#x27; and finally reach &#x27;
THE DEPARTURE (part 1 &amp; 2)
&#x27;. Their hope is none of them will be &#x27;
LEFT BEHIND II
&#x27; face the rapture!

Poster [Preview] Created by: Dukkie

Six map campaign:

-------------------------
1. Train Station
2. Military Base
3. The Forest
4. The Terrain
5. The Departure (Part 1)
6. Final Departure (Part 2)

======================================
Except under the accounts &quot;ilcannibal&quot; or &quot;Trunten&quot; or &quot;Time Lord Magnum&quot; or &quot;1sg Heartless&quot;</div>
    ]]></description>
      <content:encoded><![CDATA[
    
    <style>
        .gmLightboxOpen {
            overflow: hidden;
        }

        .gm-actions {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
            margin: 0 0 12px 0;
        }

        .gm-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-height: 38px;
            padding: 8px 14px;
            border-radius: 8px;
            text-decoration: none;
            color: #ffffff;
            font-weight: 600;
            font-size: 14px;
        }

        .gm-btn-download {
            background: #1d9f61;
        }

        .gm-btn-steam {
            background: #1d5fb5;
        }

        .gm-media-label {
            margin: 6px 0 8px 0;
            font-size: 14px;
            font-weight: 600;
            letter-spacing: .2px;
        }

        .cleanSpoilerGallery {
            margin: 14px 0;
            border-radius: 10px;
            overflow: hidden;
        }

        .cleanSpoilerGallery .ipsSpoiler_header {
            padding: 11px 14px;
            background: linear-gradient(180deg, rgba(255,255,255,.07), rgba(255,255,255,.03));
            border: 1px solid rgba(255,255,255,.08);
            border-bottom: 0;
            border-radius: 10px 10px 0 0;
        }

        .cleanSpoilerGallery .ipsSpoiler_header span {
            font-size: 13px;
            font-weight: 600;
            letter-spacing: .2px;
            color: #e8e8e8;
        }

        .cleanSpoilerGallery .ipsSpoiler_contents {
            padding: 12px;
            background: rgba(255,255,255,.035);
            border: 1px solid rgba(255,255,255,.08);
            border-radius: 0 0 10px 10px;
        }

        .cleanSpoilerGallery .spoilerGalleryGrid {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            align-items: flex-start;
        }

        .cleanSpoilerGallery .spoilerGalleryItem {
            display: inline-block;
            overflow: hidden;
            border-radius: 8px;
            background: #111;
            border: 1px solid rgba(255,255,255,.07);
            width: 188px;
            text-decoration: none;
        }

        .cleanSpoilerGallery .spoilerGalleryItem img {
            width: 100%;
            height: 112px;
            object-fit: cover;
            display: block;
            border-radius: 8px;
            transition: transform .25s ease, filter .25s ease;
        }

        .cleanSpoilerGallery .spoilerGalleryItem:hover img {
            transform: scale(1.025);
            filter: brightness(1.06);
        }

        .gm-description-text {
            white-space: pre-wrap;
            line-height: 1.6;
            font-size: 15px;
        }

        .gm-description-text a {
            color: #1d5fb5;
            text-decoration: underline;
        }

        .gm-description-text a.gm-inline-steam {
            display: inline-block;
            margin-top: 6px;
            padding: 3px 8px;
            border-radius: 6px;
            border: 1px solid #1d5fb5;
            text-decoration: none;
        }

        .gmLightbox {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, .86);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 2147483640;
            padding: 20px;
        }

        .gmLightbox.is-open {
            display: flex;
        }

        .gmLightboxImage {
            max-width: min(92vw, 1500px);
            max-height: 84vh;
            border-radius: 8px;
            object-fit: contain;
            box-shadow: 0 12px 36px rgba(0, 0, 0, .5);
        }

        .gmLightboxBtn {
            position: fixed;
            border: 0;
            background: rgba(22, 22, 22, .75);
            color: #fff;
            cursor: pointer;
            border-radius: 8px;
            font-size: 22px;
            line-height: 1;
            padding: 10px 12px;
        }

        .gmLightboxClose {
            top: 16px;
            right: 16px;
            font-size: 28px;
            padding: 8px 12px;
        }

        .gmLightboxPrev {
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxNext {
            right: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxCounter {
            position: fixed;
            bottom: 16px;
            left: 50%;
            transform: translateX(-50%);
            color: #fff;
            background: rgba(18, 18, 18, .72);
            border-radius: 999px;
            padding: 6px 12px;
            font-size: 12px;
        }

        @media (max-width: 640px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: calc(50% - 5px);
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 105px;
            }

            .gm-btn {
                width: 100%;
            }

            .gmLightboxPrev,
            .gmLightboxNext {
                font-size: 18px;
                padding: 8px 10px;
            }
        }

        @media (max-width: 420px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: 100%;
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 150px;
            }
        }
    </style>
    
    <div class="gm-actions"><a class="gm-btn gm-btn-download" href="https://www.gamemaps.com/download/eyJpZHMiOlszNTYwMV19?s=96a58604a08b4925e3e0c3216e9b3801152f741f619d32ce6336e3497d57e707&amp;e=1778471271" target="_blank" rel="nofollow noopener noreferrer">Direct Download</a></div>
    
    <div class="gm-media-label">Media</div>
    <div class="ipsSpoiler cleanSpoilerGallery" data-ipsspoiler>
        <div class="ipsSpoiler_header">
            <span>Media gallery</span>
        </div>
        <div class="ipsSpoiler_contents ipsClearfix">
            <div class="spoilerGalleryGrid">
                
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_0.jpg?1776217472"
               data-gm-gallery="gm-gallery-35601"
               data-gm-index="0"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35601"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_0.jpg?1776217472" alt="GameMaps media 1" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_5.jpg?1776279478"
               data-gm-gallery="gm-gallery-35601"
               data-gm-index="1"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35601"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_5.jpg?1776279478" alt="GameMaps media 2" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_1.jpg?1776217477"
               data-gm-gallery="gm-gallery-35601"
               data-gm-index="2"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35601"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_1.jpg?1776217477" alt="GameMaps media 3" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_2.jpg?1776217483"
               data-gm-gallery="gm-gallery-35601"
               data-gm-index="3"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35601"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_2.jpg?1776217483" alt="GameMaps media 4" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_3.jpg?1776217490"
               data-gm-gallery="gm-gallery-35601"
               data-gm-index="4"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35601"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_3.jpg?1776217490" alt="GameMaps media 5" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_4.jpg?1776217499"
               data-gm-gallery="gm-gallery-35601"
               data-gm-index="5"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35601"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_4.jpg?1776217499" alt="GameMaps media 6" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_6.jpg?1776663583"
               data-gm-gallery="gm-gallery-35601"
               data-gm-index="6"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35601"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_6.jpg?1776663583" alt="GameMaps media 7" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_7.jpg?1777393526"
               data-gm-gallery="gm-gallery-35601"
               data-gm-index="7"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35601"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/final_departure_35601_7.jpg?1777393526" alt="GameMaps media 8" loading="lazy">
            </a>
            
            </div>
        </div>
    </div>

    <div class="gmLightbox" data-gm-lightbox="gm-gallery-35601" hidden>
        <button type="button" class="gmLightboxBtn gmLightboxClose" aria-label="Close">&times;</button>
        <button type="button" class="gmLightboxBtn gmLightboxPrev" aria-label="Previous image">&#10094;</button>
        <img class="gmLightboxImage" src="" alt="">
        <button type="button" class="gmLightboxBtn gmLightboxNext" aria-label="Next image">&#10095;</button>
        <div class="gmLightboxCounter">1 / 8</div>
    </div>

    
    <script>
    (function () {
        if (window.__gmGalleryInitialized) {
            return;
        }
        window.__gmGalleryInitialized = true;

        function getOpenModal() {
            return document.querySelector('.gmLightbox.is-open');
        }

        function getGalleryLinks(group) {
            var allLinks = Array.prototype.slice.call(document.querySelectorAll('a[data-gm-gallery]'));
            return allLinks.filter(function (link) {
                return link.getAttribute('data-gm-gallery') === group;
            });
        }

        function closeModal(modal) {
            if (!modal) {
                return;
            }
            modal.classList.remove('is-open');
            modal.setAttribute('hidden', 'hidden');
            var image = modal.querySelector('.gmLightboxImage');
            if (image) {
                image.removeAttribute('src');
            }
            document.documentElement.classList.remove('gmLightboxOpen');
        }

        function renderModal(modal) {
            var links = modal.__gmLinks || [];
            if (!links.length) {
                return;
            }

            var index = modal.__gmIndex || 0;
            if (index < 0) {
                index = links.length - 1;
            }
            if (index >= links.length) {
                index = 0;
            }

            modal.__gmIndex = index;
            var activeLink = links[index];
            var image = modal.querySelector('.gmLightboxImage');
            var thumb = activeLink.querySelector('img');
            var counter = modal.querySelector('.gmLightboxCounter');

            if (image) {
                image.setAttribute('src', activeLink.getAttribute('href'));
                image.setAttribute('alt', thumb ? (thumb.getAttribute('alt') || '') : '');
            }

            if (counter) {
                counter.textContent = (index + 1) + ' / ' + links.length;
            }
        }

        function openModal(modal, links, startIndex) {
            if (!modal || !links.length) {
                return;
            }
            modal.__gmLinks = links;
            modal.__gmIndex = startIndex;
            renderModal(modal);
            modal.removeAttribute('hidden');
            modal.classList.add('is-open');
            document.documentElement.classList.add('gmLightboxOpen');
        }

        function stepModal(modal, delta) {
            if (!modal) {
                return;
            }
            modal.__gmIndex = (modal.__gmIndex || 0) + delta;
            renderModal(modal);
        }

        document.addEventListener('click', function (event) {
            var galleryItem = event.target.closest('a[data-gm-gallery]');
            if (galleryItem) {
                var group = galleryItem.getAttribute('data-gm-gallery');
                var modal = document.querySelector('.gmLightbox[data-gm-lightbox="' + group + '"]');
                var links = getGalleryLinks(group);
                var startIndex = parseInt(galleryItem.getAttribute('data-gm-index') || '0', 10);
                if (modal && links.length) {
                    event.preventDefault();
                    openModal(modal, links, startIndex);
                }
                return;
            }

            var closeButton = event.target.closest('.gmLightboxClose');
            if (closeButton) {
                closeModal(closeButton.closest('.gmLightbox'));
                return;
            }

            var prevButton = event.target.closest('.gmLightboxPrev');
            if (prevButton) {
                stepModal(prevButton.closest('.gmLightbox'), -1);
                return;
            }

            var nextButton = event.target.closest('.gmLightboxNext');
            if (nextButton) {
                stepModal(nextButton.closest('.gmLightbox'), 1);
                return;
            }

            var openModalElement = event.target.classList.contains('gmLightbox') ? event.target : null;
            if (openModalElement) {
                closeModal(openModalElement);
            }
        });

        document.addEventListener('keydown', function (event) {
            var modal = getOpenModal();
            if (!modal) {
                return;
            }

            if (event.key === 'Escape') {
                closeModal(modal);
            } else if (event.key === 'ArrowLeft') {
                stepModal(modal, -1);
            } else if (event.key === 'ArrowRight') {
                stepModal(modal, 1);
            }
        });
    })();
    </script>
    
    
    <div class="gm-description-text">Left Behind II - Final Departure

&quot;Who will survive?&quot;

The survivors begin they journey from &#x27;
THE STATION
&#x27; and &#x27;
MILITARY BASE
&#x27; they must survive &#x27;
THE FOREST
&#x27; through &#x27;
THE TERRAIN
&#x27; and finally reach &#x27;
THE DEPARTURE (part 1 &amp; 2)
&#x27;. Their hope is none of them will be &#x27;
LEFT BEHIND II
&#x27; face the rapture!

Poster [Preview] Created by: Dukkie

Six map campaign:

-------------------------
1. Train Station
2. Military Base
3. The Forest
4. The Terrain
5. The Departure (Part 1)
6. Final Departure (Part 2)

======================================
Except under the accounts &quot;ilcannibal&quot; or &quot;Trunten&quot; or &quot;Time Lord Magnum&quot; or &quot;1sg Heartless&quot;</div>
    ]]></content:encoded>
      <pubDate>Sat, 09 May 2026 03:07:18 +0000</pubDate>
    </item>
    <item>
      <title>[ Mod ] MAC-10 Bolt Locking Sound Removal</title>
      <link>https://www.gamemaps.com/details/35830</link>
      <guid>https://www.gamemaps.com/details/35830</guid>
      <description><![CDATA[
    
    <style>
        .gmLightboxOpen {
            overflow: hidden;
        }

        .gm-actions {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
            margin: 0 0 12px 0;
        }

        .gm-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-height: 38px;
            padding: 8px 14px;
            border-radius: 8px;
            text-decoration: none;
            color: #ffffff;
            font-weight: 600;
            font-size: 14px;
        }

        .gm-btn-download {
            background: #1d9f61;
        }

        .gm-btn-steam {
            background: #1d5fb5;
        }

        .gm-media-label {
            margin: 6px 0 8px 0;
            font-size: 14px;
            font-weight: 600;
            letter-spacing: .2px;
        }

        .cleanSpoilerGallery {
            margin: 14px 0;
            border-radius: 10px;
            overflow: hidden;
        }

        .cleanSpoilerGallery .ipsSpoiler_header {
            padding: 11px 14px;
            background: linear-gradient(180deg, rgba(255,255,255,.07), rgba(255,255,255,.03));
            border: 1px solid rgba(255,255,255,.08);
            border-bottom: 0;
            border-radius: 10px 10px 0 0;
        }

        .cleanSpoilerGallery .ipsSpoiler_header span {
            font-size: 13px;
            font-weight: 600;
            letter-spacing: .2px;
            color: #e8e8e8;
        }

        .cleanSpoilerGallery .ipsSpoiler_contents {
            padding: 12px;
            background: rgba(255,255,255,.035);
            border: 1px solid rgba(255,255,255,.08);
            border-radius: 0 0 10px 10px;
        }

        .cleanSpoilerGallery .spoilerGalleryGrid {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            align-items: flex-start;
        }

        .cleanSpoilerGallery .spoilerGalleryItem {
            display: inline-block;
            overflow: hidden;
            border-radius: 8px;
            background: #111;
            border: 1px solid rgba(255,255,255,.07);
            width: 188px;
            text-decoration: none;
        }

        .cleanSpoilerGallery .spoilerGalleryItem img {
            width: 100%;
            height: 112px;
            object-fit: cover;
            display: block;
            border-radius: 8px;
            transition: transform .25s ease, filter .25s ease;
        }

        .cleanSpoilerGallery .spoilerGalleryItem:hover img {
            transform: scale(1.025);
            filter: brightness(1.06);
        }

        .gm-description-text {
            white-space: pre-wrap;
            line-height: 1.6;
            font-size: 15px;
        }

        .gm-description-text a {
            color: #1d5fb5;
            text-decoration: underline;
        }

        .gm-description-text a.gm-inline-steam {
            display: inline-block;
            margin-top: 6px;
            padding: 3px 8px;
            border-radius: 6px;
            border: 1px solid #1d5fb5;
            text-decoration: none;
        }

        .gmLightbox {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, .86);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 2147483640;
            padding: 20px;
        }

        .gmLightbox.is-open {
            display: flex;
        }

        .gmLightboxImage {
            max-width: min(92vw, 1500px);
            max-height: 84vh;
            border-radius: 8px;
            object-fit: contain;
            box-shadow: 0 12px 36px rgba(0, 0, 0, .5);
        }

        .gmLightboxBtn {
            position: fixed;
            border: 0;
            background: rgba(22, 22, 22, .75);
            color: #fff;
            cursor: pointer;
            border-radius: 8px;
            font-size: 22px;
            line-height: 1;
            padding: 10px 12px;
        }

        .gmLightboxClose {
            top: 16px;
            right: 16px;
            font-size: 28px;
            padding: 8px 12px;
        }

        .gmLightboxPrev {
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxNext {
            right: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxCounter {
            position: fixed;
            bottom: 16px;
            left: 50%;
            transform: translateX(-50%);
            color: #fff;
            background: rgba(18, 18, 18, .72);
            border-radius: 999px;
            padding: 6px 12px;
            font-size: 12px;
        }

        @media (max-width: 640px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: calc(50% - 5px);
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 105px;
            }

            .gm-btn {
                width: 100%;
            }

            .gmLightboxPrev,
            .gmLightboxNext {
                font-size: 18px;
                padding: 8px 10px;
            }
        }

        @media (max-width: 420px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: 100%;
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 150px;
            }
        }
    </style>
    
    <div class="gm-actions"><a class="gm-btn gm-btn-download" href="https://www.gamemaps.com/download/eyJpZHMiOlszNTgzMF19?s=ac47f128c79a53383049cc340c042c895ebf2fd60486e58023fe04d1e335c183&amp;e=1778471271" target="_blank" rel="nofollow noopener noreferrer">Direct Download</a><a class="gm-btn gm-btn-steam" href="https://steamcommunity.com/sharedfiles/filedetails/?id=3721512313" target="_blank" rel="nofollow noopener noreferrer">Steam Workshop</a></div>
    
    <div class="gm-media-label">Media</div>
    <div class="ipsSpoiler cleanSpoilerGallery" data-ipsspoiler>
        <div class="ipsSpoiler_header">
            <span>Media gallery</span>
        </div>
        <div class="ipsSpoiler_contents ipsClearfix">
            <div class="spoilerGalleryGrid">
                
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/mac-10_bolt_locking_sound_removal_35830_0.jpg?1778208090"
               data-gm-gallery="gm-gallery-35830"
               data-gm-index="0"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35830"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/mac-10_bolt_locking_sound_removal_35830_0.jpg?1778208090" alt="GameMaps media 1" loading="lazy">
            </a>
            
            </div>
        </div>
    </div>

    <div class="gmLightbox" data-gm-lightbox="gm-gallery-35830" hidden>
        <button type="button" class="gmLightboxBtn gmLightboxClose" aria-label="Close">&times;</button>
        <button type="button" class="gmLightboxBtn gmLightboxPrev" aria-label="Previous image">&#10094;</button>
        <img class="gmLightboxImage" src="" alt="">
        <button type="button" class="gmLightboxBtn gmLightboxNext" aria-label="Next image">&#10095;</button>
        <div class="gmLightboxCounter">1 / 1</div>
    </div>

    
    <script>
    (function () {
        if (window.__gmGalleryInitialized) {
            return;
        }
        window.__gmGalleryInitialized = true;

        function getOpenModal() {
            return document.querySelector('.gmLightbox.is-open');
        }

        function getGalleryLinks(group) {
            var allLinks = Array.prototype.slice.call(document.querySelectorAll('a[data-gm-gallery]'));
            return allLinks.filter(function (link) {
                return link.getAttribute('data-gm-gallery') === group;
            });
        }

        function closeModal(modal) {
            if (!modal) {
                return;
            }
            modal.classList.remove('is-open');
            modal.setAttribute('hidden', 'hidden');
            var image = modal.querySelector('.gmLightboxImage');
            if (image) {
                image.removeAttribute('src');
            }
            document.documentElement.classList.remove('gmLightboxOpen');
        }

        function renderModal(modal) {
            var links = modal.__gmLinks || [];
            if (!links.length) {
                return;
            }

            var index = modal.__gmIndex || 0;
            if (index < 0) {
                index = links.length - 1;
            }
            if (index >= links.length) {
                index = 0;
            }

            modal.__gmIndex = index;
            var activeLink = links[index];
            var image = modal.querySelector('.gmLightboxImage');
            var thumb = activeLink.querySelector('img');
            var counter = modal.querySelector('.gmLightboxCounter');

            if (image) {
                image.setAttribute('src', activeLink.getAttribute('href'));
                image.setAttribute('alt', thumb ? (thumb.getAttribute('alt') || '') : '');
            }

            if (counter) {
                counter.textContent = (index + 1) + ' / ' + links.length;
            }
        }

        function openModal(modal, links, startIndex) {
            if (!modal || !links.length) {
                return;
            }
            modal.__gmLinks = links;
            modal.__gmIndex = startIndex;
            renderModal(modal);
            modal.removeAttribute('hidden');
            modal.classList.add('is-open');
            document.documentElement.classList.add('gmLightboxOpen');
        }

        function stepModal(modal, delta) {
            if (!modal) {
                return;
            }
            modal.__gmIndex = (modal.__gmIndex || 0) + delta;
            renderModal(modal);
        }

        document.addEventListener('click', function (event) {
            var galleryItem = event.target.closest('a[data-gm-gallery]');
            if (galleryItem) {
                var group = galleryItem.getAttribute('data-gm-gallery');
                var modal = document.querySelector('.gmLightbox[data-gm-lightbox="' + group + '"]');
                var links = getGalleryLinks(group);
                var startIndex = parseInt(galleryItem.getAttribute('data-gm-index') || '0', 10);
                if (modal && links.length) {
                    event.preventDefault();
                    openModal(modal, links, startIndex);
                }
                return;
            }

            var closeButton = event.target.closest('.gmLightboxClose');
            if (closeButton) {
                closeModal(closeButton.closest('.gmLightbox'));
                return;
            }

            var prevButton = event.target.closest('.gmLightboxPrev');
            if (prevButton) {
                stepModal(prevButton.closest('.gmLightbox'), -1);
                return;
            }

            var nextButton = event.target.closest('.gmLightboxNext');
            if (nextButton) {
                stepModal(nextButton.closest('.gmLightbox'), 1);
                return;
            }

            var openModalElement = event.target.classList.contains('gmLightbox') ? event.target : null;
            if (openModalElement) {
                closeModal(openModalElement);
            }
        });

        document.addEventListener('keydown', function (event) {
            var modal = getOpenModal();
            if (!modal) {
                return;
            }

            if (event.key === 'Escape') {
                closeModal(modal);
            } else if (event.key === 'ArrowLeft') {
                stepModal(modal, -1);
            } else if (event.key === 'ArrowRight') {
                stepModal(modal, 1);
            }
        });
    })();
    </script>
    
    
    <div class="gm-description-text">This removes the bolt locking sound from the MAC-10 deploy animation, since the gun is open bolt.

Alternate file sources:

Steam Workshop</div>
    ]]></description>
      <content:encoded><![CDATA[
    
    <style>
        .gmLightboxOpen {
            overflow: hidden;
        }

        .gm-actions {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
            margin: 0 0 12px 0;
        }

        .gm-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-height: 38px;
            padding: 8px 14px;
            border-radius: 8px;
            text-decoration: none;
            color: #ffffff;
            font-weight: 600;
            font-size: 14px;
        }

        .gm-btn-download {
            background: #1d9f61;
        }

        .gm-btn-steam {
            background: #1d5fb5;
        }

        .gm-media-label {
            margin: 6px 0 8px 0;
            font-size: 14px;
            font-weight: 600;
            letter-spacing: .2px;
        }

        .cleanSpoilerGallery {
            margin: 14px 0;
            border-radius: 10px;
            overflow: hidden;
        }

        .cleanSpoilerGallery .ipsSpoiler_header {
            padding: 11px 14px;
            background: linear-gradient(180deg, rgba(255,255,255,.07), rgba(255,255,255,.03));
            border: 1px solid rgba(255,255,255,.08);
            border-bottom: 0;
            border-radius: 10px 10px 0 0;
        }

        .cleanSpoilerGallery .ipsSpoiler_header span {
            font-size: 13px;
            font-weight: 600;
            letter-spacing: .2px;
            color: #e8e8e8;
        }

        .cleanSpoilerGallery .ipsSpoiler_contents {
            padding: 12px;
            background: rgba(255,255,255,.035);
            border: 1px solid rgba(255,255,255,.08);
            border-radius: 0 0 10px 10px;
        }

        .cleanSpoilerGallery .spoilerGalleryGrid {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            align-items: flex-start;
        }

        .cleanSpoilerGallery .spoilerGalleryItem {
            display: inline-block;
            overflow: hidden;
            border-radius: 8px;
            background: #111;
            border: 1px solid rgba(255,255,255,.07);
            width: 188px;
            text-decoration: none;
        }

        .cleanSpoilerGallery .spoilerGalleryItem img {
            width: 100%;
            height: 112px;
            object-fit: cover;
            display: block;
            border-radius: 8px;
            transition: transform .25s ease, filter .25s ease;
        }

        .cleanSpoilerGallery .spoilerGalleryItem:hover img {
            transform: scale(1.025);
            filter: brightness(1.06);
        }

        .gm-description-text {
            white-space: pre-wrap;
            line-height: 1.6;
            font-size: 15px;
        }

        .gm-description-text a {
            color: #1d5fb5;
            text-decoration: underline;
        }

        .gm-description-text a.gm-inline-steam {
            display: inline-block;
            margin-top: 6px;
            padding: 3px 8px;
            border-radius: 6px;
            border: 1px solid #1d5fb5;
            text-decoration: none;
        }

        .gmLightbox {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, .86);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 2147483640;
            padding: 20px;
        }

        .gmLightbox.is-open {
            display: flex;
        }

        .gmLightboxImage {
            max-width: min(92vw, 1500px);
            max-height: 84vh;
            border-radius: 8px;
            object-fit: contain;
            box-shadow: 0 12px 36px rgba(0, 0, 0, .5);
        }

        .gmLightboxBtn {
            position: fixed;
            border: 0;
            background: rgba(22, 22, 22, .75);
            color: #fff;
            cursor: pointer;
            border-radius: 8px;
            font-size: 22px;
            line-height: 1;
            padding: 10px 12px;
        }

        .gmLightboxClose {
            top: 16px;
            right: 16px;
            font-size: 28px;
            padding: 8px 12px;
        }

        .gmLightboxPrev {
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxNext {
            right: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxCounter {
            position: fixed;
            bottom: 16px;
            left: 50%;
            transform: translateX(-50%);
            color: #fff;
            background: rgba(18, 18, 18, .72);
            border-radius: 999px;
            padding: 6px 12px;
            font-size: 12px;
        }

        @media (max-width: 640px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: calc(50% - 5px);
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 105px;
            }

            .gm-btn {
                width: 100%;
            }

            .gmLightboxPrev,
            .gmLightboxNext {
                font-size: 18px;
                padding: 8px 10px;
            }
        }

        @media (max-width: 420px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: 100%;
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 150px;
            }
        }
    </style>
    
    <div class="gm-actions"><a class="gm-btn gm-btn-download" href="https://www.gamemaps.com/download/eyJpZHMiOlszNTgzMF19?s=ac47f128c79a53383049cc340c042c895ebf2fd60486e58023fe04d1e335c183&amp;e=1778471271" target="_blank" rel="nofollow noopener noreferrer">Direct Download</a><a class="gm-btn gm-btn-steam" href="https://steamcommunity.com/sharedfiles/filedetails/?id=3721512313" target="_blank" rel="nofollow noopener noreferrer">Steam Workshop</a></div>
    
    <div class="gm-media-label">Media</div>
    <div class="ipsSpoiler cleanSpoilerGallery" data-ipsspoiler>
        <div class="ipsSpoiler_header">
            <span>Media gallery</span>
        </div>
        <div class="ipsSpoiler_contents ipsClearfix">
            <div class="spoilerGalleryGrid">
                
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/mac-10_bolt_locking_sound_removal_35830_0.jpg?1778208090"
               data-gm-gallery="gm-gallery-35830"
               data-gm-index="0"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35830"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/mac-10_bolt_locking_sound_removal_35830_0.jpg?1778208090" alt="GameMaps media 1" loading="lazy">
            </a>
            
            </div>
        </div>
    </div>

    <div class="gmLightbox" data-gm-lightbox="gm-gallery-35830" hidden>
        <button type="button" class="gmLightboxBtn gmLightboxClose" aria-label="Close">&times;</button>
        <button type="button" class="gmLightboxBtn gmLightboxPrev" aria-label="Previous image">&#10094;</button>
        <img class="gmLightboxImage" src="" alt="">
        <button type="button" class="gmLightboxBtn gmLightboxNext" aria-label="Next image">&#10095;</button>
        <div class="gmLightboxCounter">1 / 1</div>
    </div>

    
    <script>
    (function () {
        if (window.__gmGalleryInitialized) {
            return;
        }
        window.__gmGalleryInitialized = true;

        function getOpenModal() {
            return document.querySelector('.gmLightbox.is-open');
        }

        function getGalleryLinks(group) {
            var allLinks = Array.prototype.slice.call(document.querySelectorAll('a[data-gm-gallery]'));
            return allLinks.filter(function (link) {
                return link.getAttribute('data-gm-gallery') === group;
            });
        }

        function closeModal(modal) {
            if (!modal) {
                return;
            }
            modal.classList.remove('is-open');
            modal.setAttribute('hidden', 'hidden');
            var image = modal.querySelector('.gmLightboxImage');
            if (image) {
                image.removeAttribute('src');
            }
            document.documentElement.classList.remove('gmLightboxOpen');
        }

        function renderModal(modal) {
            var links = modal.__gmLinks || [];
            if (!links.length) {
                return;
            }

            var index = modal.__gmIndex || 0;
            if (index < 0) {
                index = links.length - 1;
            }
            if (index >= links.length) {
                index = 0;
            }

            modal.__gmIndex = index;
            var activeLink = links[index];
            var image = modal.querySelector('.gmLightboxImage');
            var thumb = activeLink.querySelector('img');
            var counter = modal.querySelector('.gmLightboxCounter');

            if (image) {
                image.setAttribute('src', activeLink.getAttribute('href'));
                image.setAttribute('alt', thumb ? (thumb.getAttribute('alt') || '') : '');
            }

            if (counter) {
                counter.textContent = (index + 1) + ' / ' + links.length;
            }
        }

        function openModal(modal, links, startIndex) {
            if (!modal || !links.length) {
                return;
            }
            modal.__gmLinks = links;
            modal.__gmIndex = startIndex;
            renderModal(modal);
            modal.removeAttribute('hidden');
            modal.classList.add('is-open');
            document.documentElement.classList.add('gmLightboxOpen');
        }

        function stepModal(modal, delta) {
            if (!modal) {
                return;
            }
            modal.__gmIndex = (modal.__gmIndex || 0) + delta;
            renderModal(modal);
        }

        document.addEventListener('click', function (event) {
            var galleryItem = event.target.closest('a[data-gm-gallery]');
            if (galleryItem) {
                var group = galleryItem.getAttribute('data-gm-gallery');
                var modal = document.querySelector('.gmLightbox[data-gm-lightbox="' + group + '"]');
                var links = getGalleryLinks(group);
                var startIndex = parseInt(galleryItem.getAttribute('data-gm-index') || '0', 10);
                if (modal && links.length) {
                    event.preventDefault();
                    openModal(modal, links, startIndex);
                }
                return;
            }

            var closeButton = event.target.closest('.gmLightboxClose');
            if (closeButton) {
                closeModal(closeButton.closest('.gmLightbox'));
                return;
            }

            var prevButton = event.target.closest('.gmLightboxPrev');
            if (prevButton) {
                stepModal(prevButton.closest('.gmLightbox'), -1);
                return;
            }

            var nextButton = event.target.closest('.gmLightboxNext');
            if (nextButton) {
                stepModal(nextButton.closest('.gmLightbox'), 1);
                return;
            }

            var openModalElement = event.target.classList.contains('gmLightbox') ? event.target : null;
            if (openModalElement) {
                closeModal(openModalElement);
            }
        });

        document.addEventListener('keydown', function (event) {
            var modal = getOpenModal();
            if (!modal) {
                return;
            }

            if (event.key === 'Escape') {
                closeModal(modal);
            } else if (event.key === 'ArrowLeft') {
                stepModal(modal, -1);
            } else if (event.key === 'ArrowRight') {
                stepModal(modal, 1);
            }
        });
    })();
    </script>
    
    
    <div class="gm-description-text">This removes the bolt locking sound from the MAC-10 deploy animation, since the gun is open bolt.

Alternate file sources:

Steam Workshop</div>
    ]]></content:encoded>
      <pubDate>Fri, 08 May 2026 02:43:12 +0000</pubDate>
    </item>
    <item>
      <title>[ Mod ] Fireworks FX Duration Fix</title>
      <link>https://www.gamemaps.com/details/35829</link>
      <guid>https://www.gamemaps.com/details/35829</guid>
      <description><![CDATA[
    
    <style>
        .gmLightboxOpen {
            overflow: hidden;
        }

        .gm-actions {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
            margin: 0 0 12px 0;
        }

        .gm-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-height: 38px;
            padding: 8px 14px;
            border-radius: 8px;
            text-decoration: none;
            color: #ffffff;
            font-weight: 600;
            font-size: 14px;
        }

        .gm-btn-download {
            background: #1d9f61;
        }

        .gm-btn-steam {
            background: #1d5fb5;
        }

        .gm-media-label {
            margin: 6px 0 8px 0;
            font-size: 14px;
            font-weight: 600;
            letter-spacing: .2px;
        }

        .cleanSpoilerGallery {
            margin: 14px 0;
            border-radius: 10px;
            overflow: hidden;
        }

        .cleanSpoilerGallery .ipsSpoiler_header {
            padding: 11px 14px;
            background: linear-gradient(180deg, rgba(255,255,255,.07), rgba(255,255,255,.03));
            border: 1px solid rgba(255,255,255,.08);
            border-bottom: 0;
            border-radius: 10px 10px 0 0;
        }

        .cleanSpoilerGallery .ipsSpoiler_header span {
            font-size: 13px;
            font-weight: 600;
            letter-spacing: .2px;
            color: #e8e8e8;
        }

        .cleanSpoilerGallery .ipsSpoiler_contents {
            padding: 12px;
            background: rgba(255,255,255,.035);
            border: 1px solid rgba(255,255,255,.08);
            border-radius: 0 0 10px 10px;
        }

        .cleanSpoilerGallery .spoilerGalleryGrid {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            align-items: flex-start;
        }

        .cleanSpoilerGallery .spoilerGalleryItem {
            display: inline-block;
            overflow: hidden;
            border-radius: 8px;
            background: #111;
            border: 1px solid rgba(255,255,255,.07);
            width: 188px;
            text-decoration: none;
        }

        .cleanSpoilerGallery .spoilerGalleryItem img {
            width: 100%;
            height: 112px;
            object-fit: cover;
            display: block;
            border-radius: 8px;
            transition: transform .25s ease, filter .25s ease;
        }

        .cleanSpoilerGallery .spoilerGalleryItem:hover img {
            transform: scale(1.025);
            filter: brightness(1.06);
        }

        .gm-description-text {
            white-space: pre-wrap;
            line-height: 1.6;
            font-size: 15px;
        }

        .gm-description-text a {
            color: #1d5fb5;
            text-decoration: underline;
        }

        .gm-description-text a.gm-inline-steam {
            display: inline-block;
            margin-top: 6px;
            padding: 3px 8px;
            border-radius: 6px;
            border: 1px solid #1d5fb5;
            text-decoration: none;
        }

        .gmLightbox {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, .86);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 2147483640;
            padding: 20px;
        }

        .gmLightbox.is-open {
            display: flex;
        }

        .gmLightboxImage {
            max-width: min(92vw, 1500px);
            max-height: 84vh;
            border-radius: 8px;
            object-fit: contain;
            box-shadow: 0 12px 36px rgba(0, 0, 0, .5);
        }

        .gmLightboxBtn {
            position: fixed;
            border: 0;
            background: rgba(22, 22, 22, .75);
            color: #fff;
            cursor: pointer;
            border-radius: 8px;
            font-size: 22px;
            line-height: 1;
            padding: 10px 12px;
        }

        .gmLightboxClose {
            top: 16px;
            right: 16px;
            font-size: 28px;
            padding: 8px 12px;
        }

        .gmLightboxPrev {
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxNext {
            right: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxCounter {
            position: fixed;
            bottom: 16px;
            left: 50%;
            transform: translateX(-50%);
            color: #fff;
            background: rgba(18, 18, 18, .72);
            border-radius: 999px;
            padding: 6px 12px;
            font-size: 12px;
        }

        @media (max-width: 640px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: calc(50% - 5px);
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 105px;
            }

            .gm-btn {
                width: 100%;
            }

            .gmLightboxPrev,
            .gmLightboxNext {
                font-size: 18px;
                padding: 8px 10px;
            }
        }

        @media (max-width: 420px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: 100%;
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 150px;
            }
        }
    </style>
    
    <div class="gm-actions"><a class="gm-btn gm-btn-download" href="https://www.gamemaps.com/download/eyJpZHMiOlszNTgyOV19?s=949d841092638c7df3ddcb85f056d4012ff86bcd3586619a3a3732b42cf4f76d&amp;e=1778471271" target="_blank" rel="nofollow noopener noreferrer">Direct Download</a><a class="gm-btn gm-btn-steam" href="https://steamcommunity.com/sharedfiles/filedetails/?id=3721511803" target="_blank" rel="nofollow noopener noreferrer">Steam Workshop</a></div>
    
    <div class="gm-media-label">Media</div>
    <div class="ipsSpoiler cleanSpoilerGallery" data-ipsspoiler>
        <div class="ipsSpoiler_header">
            <span>Media gallery</span>
        </div>
        <div class="ipsSpoiler_contents ipsClearfix">
            <div class="spoilerGalleryGrid">
                
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/fireworks_fx_duration_fix_35829_1.jpg?1778207710"
               data-gm-gallery="gm-gallery-35829"
               data-gm-index="0"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35829"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/fireworks_fx_duration_fix_35829_1.jpg?1778207710" alt="GameMaps media 1" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/fireworks_fx_duration_fix_35829_0.jpg?1778207681"
               data-gm-gallery="gm-gallery-35829"
               data-gm-index="1"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35829"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/fireworks_fx_duration_fix_35829_0.jpg?1778207681" alt="GameMaps media 2" loading="lazy">
            </a>
            
            </div>
        </div>
    </div>

    <div class="gmLightbox" data-gm-lightbox="gm-gallery-35829" hidden>
        <button type="button" class="gmLightboxBtn gmLightboxClose" aria-label="Close">&times;</button>
        <button type="button" class="gmLightboxBtn gmLightboxPrev" aria-label="Previous image">&#10094;</button>
        <img class="gmLightboxImage" src="" alt="">
        <button type="button" class="gmLightboxBtn gmLightboxNext" aria-label="Next image">&#10095;</button>
        <div class="gmLightboxCounter">1 / 2</div>
    </div>

    
    <script>
    (function () {
        if (window.__gmGalleryInitialized) {
            return;
        }
        window.__gmGalleryInitialized = true;

        function getOpenModal() {
            return document.querySelector('.gmLightbox.is-open');
        }

        function getGalleryLinks(group) {
            var allLinks = Array.prototype.slice.call(document.querySelectorAll('a[data-gm-gallery]'));
            return allLinks.filter(function (link) {
                return link.getAttribute('data-gm-gallery') === group;
            });
        }

        function closeModal(modal) {
            if (!modal) {
                return;
            }
            modal.classList.remove('is-open');
            modal.setAttribute('hidden', 'hidden');
            var image = modal.querySelector('.gmLightboxImage');
            if (image) {
                image.removeAttribute('src');
            }
            document.documentElement.classList.remove('gmLightboxOpen');
        }

        function renderModal(modal) {
            var links = modal.__gmLinks || [];
            if (!links.length) {
                return;
            }

            var index = modal.__gmIndex || 0;
            if (index < 0) {
                index = links.length - 1;
            }
            if (index >= links.length) {
                index = 0;
            }

            modal.__gmIndex = index;
            var activeLink = links[index];
            var image = modal.querySelector('.gmLightboxImage');
            var thumb = activeLink.querySelector('img');
            var counter = modal.querySelector('.gmLightboxCounter');

            if (image) {
                image.setAttribute('src', activeLink.getAttribute('href'));
                image.setAttribute('alt', thumb ? (thumb.getAttribute('alt') || '') : '');
            }

            if (counter) {
                counter.textContent = (index + 1) + ' / ' + links.length;
            }
        }

        function openModal(modal, links, startIndex) {
            if (!modal || !links.length) {
                return;
            }
            modal.__gmLinks = links;
            modal.__gmIndex = startIndex;
            renderModal(modal);
            modal.removeAttribute('hidden');
            modal.classList.add('is-open');
            document.documentElement.classList.add('gmLightboxOpen');
        }

        function stepModal(modal, delta) {
            if (!modal) {
                return;
            }
            modal.__gmIndex = (modal.__gmIndex || 0) + delta;
            renderModal(modal);
        }

        document.addEventListener('click', function (event) {
            var galleryItem = event.target.closest('a[data-gm-gallery]');
            if (galleryItem) {
                var group = galleryItem.getAttribute('data-gm-gallery');
                var modal = document.querySelector('.gmLightbox[data-gm-lightbox="' + group + '"]');
                var links = getGalleryLinks(group);
                var startIndex = parseInt(galleryItem.getAttribute('data-gm-index') || '0', 10);
                if (modal && links.length) {
                    event.preventDefault();
                    openModal(modal, links, startIndex);
                }
                return;
            }

            var closeButton = event.target.closest('.gmLightboxClose');
            if (closeButton) {
                closeModal(closeButton.closest('.gmLightbox'));
                return;
            }

            var prevButton = event.target.closest('.gmLightboxPrev');
            if (prevButton) {
                stepModal(prevButton.closest('.gmLightbox'), -1);
                return;
            }

            var nextButton = event.target.closest('.gmLightboxNext');
            if (nextButton) {
                stepModal(nextButton.closest('.gmLightbox'), 1);
                return;
            }

            var openModalElement = event.target.classList.contains('gmLightbox') ? event.target : null;
            if (openModalElement) {
                closeModal(openModalElement);
            }
        });

        document.addEventListener('keydown', function (event) {
            var modal = getOpenModal();
            if (!modal) {
                return;
            }

            if (event.key === 'Escape') {
                closeModal(modal);
            } else if (event.key === 'ArrowLeft') {
                stepModal(modal, -1);
            } else if (event.key === 'ArrowRight') {
                stepModal(modal, 1);
            }
        });
    })();
    </script>
    
    
    <div class="gm-description-text">This fixes the sound and particles from the fireworks fading before the damaging area vanishes.

Alternate file sources:

Steam Workshop</div>
    ]]></description>
      <content:encoded><![CDATA[
    
    <style>
        .gmLightboxOpen {
            overflow: hidden;
        }

        .gm-actions {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
            margin: 0 0 12px 0;
        }

        .gm-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-height: 38px;
            padding: 8px 14px;
            border-radius: 8px;
            text-decoration: none;
            color: #ffffff;
            font-weight: 600;
            font-size: 14px;
        }

        .gm-btn-download {
            background: #1d9f61;
        }

        .gm-btn-steam {
            background: #1d5fb5;
        }

        .gm-media-label {
            margin: 6px 0 8px 0;
            font-size: 14px;
            font-weight: 600;
            letter-spacing: .2px;
        }

        .cleanSpoilerGallery {
            margin: 14px 0;
            border-radius: 10px;
            overflow: hidden;
        }

        .cleanSpoilerGallery .ipsSpoiler_header {
            padding: 11px 14px;
            background: linear-gradient(180deg, rgba(255,255,255,.07), rgba(255,255,255,.03));
            border: 1px solid rgba(255,255,255,.08);
            border-bottom: 0;
            border-radius: 10px 10px 0 0;
        }

        .cleanSpoilerGallery .ipsSpoiler_header span {
            font-size: 13px;
            font-weight: 600;
            letter-spacing: .2px;
            color: #e8e8e8;
        }

        .cleanSpoilerGallery .ipsSpoiler_contents {
            padding: 12px;
            background: rgba(255,255,255,.035);
            border: 1px solid rgba(255,255,255,.08);
            border-radius: 0 0 10px 10px;
        }

        .cleanSpoilerGallery .spoilerGalleryGrid {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            align-items: flex-start;
        }

        .cleanSpoilerGallery .spoilerGalleryItem {
            display: inline-block;
            overflow: hidden;
            border-radius: 8px;
            background: #111;
            border: 1px solid rgba(255,255,255,.07);
            width: 188px;
            text-decoration: none;
        }

        .cleanSpoilerGallery .spoilerGalleryItem img {
            width: 100%;
            height: 112px;
            object-fit: cover;
            display: block;
            border-radius: 8px;
            transition: transform .25s ease, filter .25s ease;
        }

        .cleanSpoilerGallery .spoilerGalleryItem:hover img {
            transform: scale(1.025);
            filter: brightness(1.06);
        }

        .gm-description-text {
            white-space: pre-wrap;
            line-height: 1.6;
            font-size: 15px;
        }

        .gm-description-text a {
            color: #1d5fb5;
            text-decoration: underline;
        }

        .gm-description-text a.gm-inline-steam {
            display: inline-block;
            margin-top: 6px;
            padding: 3px 8px;
            border-radius: 6px;
            border: 1px solid #1d5fb5;
            text-decoration: none;
        }

        .gmLightbox {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, .86);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 2147483640;
            padding: 20px;
        }

        .gmLightbox.is-open {
            display: flex;
        }

        .gmLightboxImage {
            max-width: min(92vw, 1500px);
            max-height: 84vh;
            border-radius: 8px;
            object-fit: contain;
            box-shadow: 0 12px 36px rgba(0, 0, 0, .5);
        }

        .gmLightboxBtn {
            position: fixed;
            border: 0;
            background: rgba(22, 22, 22, .75);
            color: #fff;
            cursor: pointer;
            border-radius: 8px;
            font-size: 22px;
            line-height: 1;
            padding: 10px 12px;
        }

        .gmLightboxClose {
            top: 16px;
            right: 16px;
            font-size: 28px;
            padding: 8px 12px;
        }

        .gmLightboxPrev {
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxNext {
            right: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxCounter {
            position: fixed;
            bottom: 16px;
            left: 50%;
            transform: translateX(-50%);
            color: #fff;
            background: rgba(18, 18, 18, .72);
            border-radius: 999px;
            padding: 6px 12px;
            font-size: 12px;
        }

        @media (max-width: 640px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: calc(50% - 5px);
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 105px;
            }

            .gm-btn {
                width: 100%;
            }

            .gmLightboxPrev,
            .gmLightboxNext {
                font-size: 18px;
                padding: 8px 10px;
            }
        }

        @media (max-width: 420px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: 100%;
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 150px;
            }
        }
    </style>
    
    <div class="gm-actions"><a class="gm-btn gm-btn-download" href="https://www.gamemaps.com/download/eyJpZHMiOlszNTgyOV19?s=949d841092638c7df3ddcb85f056d4012ff86bcd3586619a3a3732b42cf4f76d&amp;e=1778471271" target="_blank" rel="nofollow noopener noreferrer">Direct Download</a><a class="gm-btn gm-btn-steam" href="https://steamcommunity.com/sharedfiles/filedetails/?id=3721511803" target="_blank" rel="nofollow noopener noreferrer">Steam Workshop</a></div>
    
    <div class="gm-media-label">Media</div>
    <div class="ipsSpoiler cleanSpoilerGallery" data-ipsspoiler>
        <div class="ipsSpoiler_header">
            <span>Media gallery</span>
        </div>
        <div class="ipsSpoiler_contents ipsClearfix">
            <div class="spoilerGalleryGrid">
                
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/fireworks_fx_duration_fix_35829_1.jpg?1778207710"
               data-gm-gallery="gm-gallery-35829"
               data-gm-index="0"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35829"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/fireworks_fx_duration_fix_35829_1.jpg?1778207710" alt="GameMaps media 1" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/fireworks_fx_duration_fix_35829_0.jpg?1778207681"
               data-gm-gallery="gm-gallery-35829"
               data-gm-index="1"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35829"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/fireworks_fx_duration_fix_35829_0.jpg?1778207681" alt="GameMaps media 2" loading="lazy">
            </a>
            
            </div>
        </div>
    </div>

    <div class="gmLightbox" data-gm-lightbox="gm-gallery-35829" hidden>
        <button type="button" class="gmLightboxBtn gmLightboxClose" aria-label="Close">&times;</button>
        <button type="button" class="gmLightboxBtn gmLightboxPrev" aria-label="Previous image">&#10094;</button>
        <img class="gmLightboxImage" src="" alt="">
        <button type="button" class="gmLightboxBtn gmLightboxNext" aria-label="Next image">&#10095;</button>
        <div class="gmLightboxCounter">1 / 2</div>
    </div>

    
    <script>
    (function () {
        if (window.__gmGalleryInitialized) {
            return;
        }
        window.__gmGalleryInitialized = true;

        function getOpenModal() {
            return document.querySelector('.gmLightbox.is-open');
        }

        function getGalleryLinks(group) {
            var allLinks = Array.prototype.slice.call(document.querySelectorAll('a[data-gm-gallery]'));
            return allLinks.filter(function (link) {
                return link.getAttribute('data-gm-gallery') === group;
            });
        }

        function closeModal(modal) {
            if (!modal) {
                return;
            }
            modal.classList.remove('is-open');
            modal.setAttribute('hidden', 'hidden');
            var image = modal.querySelector('.gmLightboxImage');
            if (image) {
                image.removeAttribute('src');
            }
            document.documentElement.classList.remove('gmLightboxOpen');
        }

        function renderModal(modal) {
            var links = modal.__gmLinks || [];
            if (!links.length) {
                return;
            }

            var index = modal.__gmIndex || 0;
            if (index < 0) {
                index = links.length - 1;
            }
            if (index >= links.length) {
                index = 0;
            }

            modal.__gmIndex = index;
            var activeLink = links[index];
            var image = modal.querySelector('.gmLightboxImage');
            var thumb = activeLink.querySelector('img');
            var counter = modal.querySelector('.gmLightboxCounter');

            if (image) {
                image.setAttribute('src', activeLink.getAttribute('href'));
                image.setAttribute('alt', thumb ? (thumb.getAttribute('alt') || '') : '');
            }

            if (counter) {
                counter.textContent = (index + 1) + ' / ' + links.length;
            }
        }

        function openModal(modal, links, startIndex) {
            if (!modal || !links.length) {
                return;
            }
            modal.__gmLinks = links;
            modal.__gmIndex = startIndex;
            renderModal(modal);
            modal.removeAttribute('hidden');
            modal.classList.add('is-open');
            document.documentElement.classList.add('gmLightboxOpen');
        }

        function stepModal(modal, delta) {
            if (!modal) {
                return;
            }
            modal.__gmIndex = (modal.__gmIndex || 0) + delta;
            renderModal(modal);
        }

        document.addEventListener('click', function (event) {
            var galleryItem = event.target.closest('a[data-gm-gallery]');
            if (galleryItem) {
                var group = galleryItem.getAttribute('data-gm-gallery');
                var modal = document.querySelector('.gmLightbox[data-gm-lightbox="' + group + '"]');
                var links = getGalleryLinks(group);
                var startIndex = parseInt(galleryItem.getAttribute('data-gm-index') || '0', 10);
                if (modal && links.length) {
                    event.preventDefault();
                    openModal(modal, links, startIndex);
                }
                return;
            }

            var closeButton = event.target.closest('.gmLightboxClose');
            if (closeButton) {
                closeModal(closeButton.closest('.gmLightbox'));
                return;
            }

            var prevButton = event.target.closest('.gmLightboxPrev');
            if (prevButton) {
                stepModal(prevButton.closest('.gmLightbox'), -1);
                return;
            }

            var nextButton = event.target.closest('.gmLightboxNext');
            if (nextButton) {
                stepModal(nextButton.closest('.gmLightbox'), 1);
                return;
            }

            var openModalElement = event.target.classList.contains('gmLightbox') ? event.target : null;
            if (openModalElement) {
                closeModal(openModalElement);
            }
        });

        document.addEventListener('keydown', function (event) {
            var modal = getOpenModal();
            if (!modal) {
                return;
            }

            if (event.key === 'Escape') {
                closeModal(modal);
            } else if (event.key === 'ArrowLeft') {
                stepModal(modal, -1);
            } else if (event.key === 'ArrowRight') {
                stepModal(modal, 1);
            }
        });
    })();
    </script>
    
    
    <div class="gm-description-text">This fixes the sound and particles from the fireworks fading before the damaging area vanishes.

Alternate file sources:

Steam Workshop</div>
    ]]></content:encoded>
      <pubDate>Fri, 08 May 2026 02:37:03 +0000</pubDate>
    </item>
    <item>
      <title>[ Mod ] Only COMM Ultimis Edward Richtofen (request)</title>
      <link>https://www.gamemaps.com/details/35822</link>
      <guid>https://www.gamemaps.com/details/35822</guid>
      <description><![CDATA[
    
    <style>
        .gmLightboxOpen {
            overflow: hidden;
        }

        .gm-actions {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
            margin: 0 0 12px 0;
        }

        .gm-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-height: 38px;
            padding: 8px 14px;
            border-radius: 8px;
            text-decoration: none;
            color: #ffffff;
            font-weight: 600;
            font-size: 14px;
        }

        .gm-btn-download {
            background: #1d9f61;
        }

        .gm-btn-steam {
            background: #1d5fb5;
        }

        .gm-media-label {
            margin: 6px 0 8px 0;
            font-size: 14px;
            font-weight: 600;
            letter-spacing: .2px;
        }

        .cleanSpoilerGallery {
            margin: 14px 0;
            border-radius: 10px;
            overflow: hidden;
        }

        .cleanSpoilerGallery .ipsSpoiler_header {
            padding: 11px 14px;
            background: linear-gradient(180deg, rgba(255,255,255,.07), rgba(255,255,255,.03));
            border: 1px solid rgba(255,255,255,.08);
            border-bottom: 0;
            border-radius: 10px 10px 0 0;
        }

        .cleanSpoilerGallery .ipsSpoiler_header span {
            font-size: 13px;
            font-weight: 600;
            letter-spacing: .2px;
            color: #e8e8e8;
        }

        .cleanSpoilerGallery .ipsSpoiler_contents {
            padding: 12px;
            background: rgba(255,255,255,.035);
            border: 1px solid rgba(255,255,255,.08);
            border-radius: 0 0 10px 10px;
        }

        .cleanSpoilerGallery .spoilerGalleryGrid {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            align-items: flex-start;
        }

        .cleanSpoilerGallery .spoilerGalleryItem {
            display: inline-block;
            overflow: hidden;
            border-radius: 8px;
            background: #111;
            border: 1px solid rgba(255,255,255,.07);
            width: 188px;
            text-decoration: none;
        }

        .cleanSpoilerGallery .spoilerGalleryItem img {
            width: 100%;
            height: 112px;
            object-fit: cover;
            display: block;
            border-radius: 8px;
            transition: transform .25s ease, filter .25s ease;
        }

        .cleanSpoilerGallery .spoilerGalleryItem:hover img {
            transform: scale(1.025);
            filter: brightness(1.06);
        }

        .gm-description-text {
            white-space: pre-wrap;
            line-height: 1.6;
            font-size: 15px;
        }

        .gm-description-text a {
            color: #1d5fb5;
            text-decoration: underline;
        }

        .gm-description-text a.gm-inline-steam {
            display: inline-block;
            margin-top: 6px;
            padding: 3px 8px;
            border-radius: 6px;
            border: 1px solid #1d5fb5;
            text-decoration: none;
        }

        .gmLightbox {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, .86);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 2147483640;
            padding: 20px;
        }

        .gmLightbox.is-open {
            display: flex;
        }

        .gmLightboxImage {
            max-width: min(92vw, 1500px);
            max-height: 84vh;
            border-radius: 8px;
            object-fit: contain;
            box-shadow: 0 12px 36px rgba(0, 0, 0, .5);
        }

        .gmLightboxBtn {
            position: fixed;
            border: 0;
            background: rgba(22, 22, 22, .75);
            color: #fff;
            cursor: pointer;
            border-radius: 8px;
            font-size: 22px;
            line-height: 1;
            padding: 10px 12px;
        }

        .gmLightboxClose {
            top: 16px;
            right: 16px;
            font-size: 28px;
            padding: 8px 12px;
        }

        .gmLightboxPrev {
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxNext {
            right: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxCounter {
            position: fixed;
            bottom: 16px;
            left: 50%;
            transform: translateX(-50%);
            color: #fff;
            background: rgba(18, 18, 18, .72);
            border-radius: 999px;
            padding: 6px 12px;
            font-size: 12px;
        }

        @media (max-width: 640px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: calc(50% - 5px);
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 105px;
            }

            .gm-btn {
                width: 100%;
            }

            .gmLightboxPrev,
            .gmLightboxNext {
                font-size: 18px;
                padding: 8px 10px;
            }
        }

        @media (max-width: 420px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: 100%;
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 150px;
            }
        }
    </style>
    
    <div class="gm-actions"><a class="gm-btn gm-btn-download" href="https://www.gamemaps.com/download/eyJpZHMiOlszNTgyMl19?s=21d3b7ad12bfe339d480b486f776cf7e5bc35313d0d438d75acb91e4bebcbe8c&amp;e=1778471271" target="_blank" rel="nofollow noopener noreferrer">Direct Download</a></div>
    
    <div class="gm-media-label">Media</div>
    <div class="ipsSpoiler cleanSpoilerGallery" data-ipsspoiler>
        <div class="ipsSpoiler_header">
            <span>Media gallery</span>
        </div>
        <div class="ipsSpoiler_contents ipsClearfix">
            <div class="spoilerGalleryGrid">
                
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_0.jpg?1778101722"
               data-gm-gallery="gm-gallery-35822"
               data-gm-index="0"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35822"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_0.jpg?1778101722" alt="GameMaps media 1" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_1.jpg?1778101722"
               data-gm-gallery="gm-gallery-35822"
               data-gm-index="1"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35822"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_1.jpg?1778101722" alt="GameMaps media 2" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_2.jpg?1778101722"
               data-gm-gallery="gm-gallery-35822"
               data-gm-index="2"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35822"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_2.jpg?1778101722" alt="GameMaps media 3" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_3.jpg?1778101722"
               data-gm-gallery="gm-gallery-35822"
               data-gm-index="3"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35822"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_3.jpg?1778101722" alt="GameMaps media 4" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_4.jpg?1778101722"
               data-gm-gallery="gm-gallery-35822"
               data-gm-index="4"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35822"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_4.jpg?1778101722" alt="GameMaps media 5" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_5.jpg?1778101722"
               data-gm-gallery="gm-gallery-35822"
               data-gm-index="5"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35822"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_5.jpg?1778101722" alt="GameMaps media 6" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_6.jpg?1778101722"
               data-gm-gallery="gm-gallery-35822"
               data-gm-index="6"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35822"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_6.jpg?1778101722" alt="GameMaps media 7" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_7.jpg?1778101722"
               data-gm-gallery="gm-gallery-35822"
               data-gm-index="7"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35822"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_7.jpg?1778101722" alt="GameMaps media 8" loading="lazy">
            </a>
            
            </div>
        </div>
    </div>

    <div class="gmLightbox" data-gm-lightbox="gm-gallery-35822" hidden>
        <button type="button" class="gmLightboxBtn gmLightboxClose" aria-label="Close">&times;</button>
        <button type="button" class="gmLightboxBtn gmLightboxPrev" aria-label="Previous image">&#10094;</button>
        <img class="gmLightboxImage" src="" alt="">
        <button type="button" class="gmLightboxBtn gmLightboxNext" aria-label="Next image">&#10095;</button>
        <div class="gmLightboxCounter">1 / 8</div>
    </div>

    
    <script>
    (function () {
        if (window.__gmGalleryInitialized) {
            return;
        }
        window.__gmGalleryInitialized = true;

        function getOpenModal() {
            return document.querySelector('.gmLightbox.is-open');
        }

        function getGalleryLinks(group) {
            var allLinks = Array.prototype.slice.call(document.querySelectorAll('a[data-gm-gallery]'));
            return allLinks.filter(function (link) {
                return link.getAttribute('data-gm-gallery') === group;
            });
        }

        function closeModal(modal) {
            if (!modal) {
                return;
            }
            modal.classList.remove('is-open');
            modal.setAttribute('hidden', 'hidden');
            var image = modal.querySelector('.gmLightboxImage');
            if (image) {
                image.removeAttribute('src');
            }
            document.documentElement.classList.remove('gmLightboxOpen');
        }

        function renderModal(modal) {
            var links = modal.__gmLinks || [];
            if (!links.length) {
                return;
            }

            var index = modal.__gmIndex || 0;
            if (index < 0) {
                index = links.length - 1;
            }
            if (index >= links.length) {
                index = 0;
            }

            modal.__gmIndex = index;
            var activeLink = links[index];
            var image = modal.querySelector('.gmLightboxImage');
            var thumb = activeLink.querySelector('img');
            var counter = modal.querySelector('.gmLightboxCounter');

            if (image) {
                image.setAttribute('src', activeLink.getAttribute('href'));
                image.setAttribute('alt', thumb ? (thumb.getAttribute('alt') || '') : '');
            }

            if (counter) {
                counter.textContent = (index + 1) + ' / ' + links.length;
            }
        }

        function openModal(modal, links, startIndex) {
            if (!modal || !links.length) {
                return;
            }
            modal.__gmLinks = links;
            modal.__gmIndex = startIndex;
            renderModal(modal);
            modal.removeAttribute('hidden');
            modal.classList.add('is-open');
            document.documentElement.classList.add('gmLightboxOpen');
        }

        function stepModal(modal, delta) {
            if (!modal) {
                return;
            }
            modal.__gmIndex = (modal.__gmIndex || 0) + delta;
            renderModal(modal);
        }

        document.addEventListener('click', function (event) {
            var galleryItem = event.target.closest('a[data-gm-gallery]');
            if (galleryItem) {
                var group = galleryItem.getAttribute('data-gm-gallery');
                var modal = document.querySelector('.gmLightbox[data-gm-lightbox="' + group + '"]');
                var links = getGalleryLinks(group);
                var startIndex = parseInt(galleryItem.getAttribute('data-gm-index') || '0', 10);
                if (modal && links.length) {
                    event.preventDefault();
                    openModal(modal, links, startIndex);
                }
                return;
            }

            var closeButton = event.target.closest('.gmLightboxClose');
            if (closeButton) {
                closeModal(closeButton.closest('.gmLightbox'));
                return;
            }

            var prevButton = event.target.closest('.gmLightboxPrev');
            if (prevButton) {
                stepModal(prevButton.closest('.gmLightbox'), -1);
                return;
            }

            var nextButton = event.target.closest('.gmLightboxNext');
            if (nextButton) {
                stepModal(nextButton.closest('.gmLightbox'), 1);
                return;
            }

            var openModalElement = event.target.classList.contains('gmLightbox') ? event.target : null;
            if (openModalElement) {
                closeModal(openModalElement);
            }
        });

        document.addEventListener('keydown', function (event) {
            var modal = getOpenModal();
            if (!modal) {
                return;
            }

            if (event.key === 'Escape') {
                closeModal(modal);
            } else if (event.key === 'ArrowLeft') {
                stepModal(modal, -1);
            } else if (event.key === 'ArrowRight') {
                stepModal(modal, 1);
            }
        });
    })();
    </script>
    
    
    <div class="gm-description-text">Only COMM Ultimis Edward Richtofen (request)

A character from the **** Zombies series, a bonus mode of the Call of Duty series. - Edward Richtofen (Ultimis) survivor Skin.
Replaces all survivors L4D1(bill,francis,louis,zoey),L4D2 (nick,coach,ellis,rochelle) with only zoey

Credits:
skin - EnergyFlamer [COMM OPEN] [COMM: Ultimis Edward Richtofen [Francis]]
skin,sound edit - l4dkk

9.vpk files are included in the compressed file.

COMM Ultimis Edward Richtofen bill.vpk (bill skin)
COMM Ultimis Edward Richtofen francis.vpk (francis skin)
COMM Ultimis Edward Richtofen louis.vpk (Louis skin)
COMM Ultimis Edward Richtofen zoey.vpk (zoey skin)
COMM Ultimis Edward Richtofen coach.vpk (coach skin)
COMM Ultimis Edward Richtofen elliss.vpk (ellis skin)
COMM Ultimis Edward Richtofen nick.vpk (nick skin)
COMM Ultimis Edward Richtofen rochelle.vpk (Rochelle skin)
Only COMM Ultimis Edward Richtofen.vpk (all survivors)

This skin works best on local servers and singleplayer.</div>
    ]]></description>
      <content:encoded><![CDATA[
    
    <style>
        .gmLightboxOpen {
            overflow: hidden;
        }

        .gm-actions {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
            margin: 0 0 12px 0;
        }

        .gm-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-height: 38px;
            padding: 8px 14px;
            border-radius: 8px;
            text-decoration: none;
            color: #ffffff;
            font-weight: 600;
            font-size: 14px;
        }

        .gm-btn-download {
            background: #1d9f61;
        }

        .gm-btn-steam {
            background: #1d5fb5;
        }

        .gm-media-label {
            margin: 6px 0 8px 0;
            font-size: 14px;
            font-weight: 600;
            letter-spacing: .2px;
        }

        .cleanSpoilerGallery {
            margin: 14px 0;
            border-radius: 10px;
            overflow: hidden;
        }

        .cleanSpoilerGallery .ipsSpoiler_header {
            padding: 11px 14px;
            background: linear-gradient(180deg, rgba(255,255,255,.07), rgba(255,255,255,.03));
            border: 1px solid rgba(255,255,255,.08);
            border-bottom: 0;
            border-radius: 10px 10px 0 0;
        }

        .cleanSpoilerGallery .ipsSpoiler_header span {
            font-size: 13px;
            font-weight: 600;
            letter-spacing: .2px;
            color: #e8e8e8;
        }

        .cleanSpoilerGallery .ipsSpoiler_contents {
            padding: 12px;
            background: rgba(255,255,255,.035);
            border: 1px solid rgba(255,255,255,.08);
            border-radius: 0 0 10px 10px;
        }

        .cleanSpoilerGallery .spoilerGalleryGrid {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            align-items: flex-start;
        }

        .cleanSpoilerGallery .spoilerGalleryItem {
            display: inline-block;
            overflow: hidden;
            border-radius: 8px;
            background: #111;
            border: 1px solid rgba(255,255,255,.07);
            width: 188px;
            text-decoration: none;
        }

        .cleanSpoilerGallery .spoilerGalleryItem img {
            width: 100%;
            height: 112px;
            object-fit: cover;
            display: block;
            border-radius: 8px;
            transition: transform .25s ease, filter .25s ease;
        }

        .cleanSpoilerGallery .spoilerGalleryItem:hover img {
            transform: scale(1.025);
            filter: brightness(1.06);
        }

        .gm-description-text {
            white-space: pre-wrap;
            line-height: 1.6;
            font-size: 15px;
        }

        .gm-description-text a {
            color: #1d5fb5;
            text-decoration: underline;
        }

        .gm-description-text a.gm-inline-steam {
            display: inline-block;
            margin-top: 6px;
            padding: 3px 8px;
            border-radius: 6px;
            border: 1px solid #1d5fb5;
            text-decoration: none;
        }

        .gmLightbox {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, .86);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 2147483640;
            padding: 20px;
        }

        .gmLightbox.is-open {
            display: flex;
        }

        .gmLightboxImage {
            max-width: min(92vw, 1500px);
            max-height: 84vh;
            border-radius: 8px;
            object-fit: contain;
            box-shadow: 0 12px 36px rgba(0, 0, 0, .5);
        }

        .gmLightboxBtn {
            position: fixed;
            border: 0;
            background: rgba(22, 22, 22, .75);
            color: #fff;
            cursor: pointer;
            border-radius: 8px;
            font-size: 22px;
            line-height: 1;
            padding: 10px 12px;
        }

        .gmLightboxClose {
            top: 16px;
            right: 16px;
            font-size: 28px;
            padding: 8px 12px;
        }

        .gmLightboxPrev {
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxNext {
            right: 16px;
            top: 50%;
            transform: translateY(-50%);
        }

        .gmLightboxCounter {
            position: fixed;
            bottom: 16px;
            left: 50%;
            transform: translateX(-50%);
            color: #fff;
            background: rgba(18, 18, 18, .72);
            border-radius: 999px;
            padding: 6px 12px;
            font-size: 12px;
        }

        @media (max-width: 640px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: calc(50% - 5px);
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 105px;
            }

            .gm-btn {
                width: 100%;
            }

            .gmLightboxPrev,
            .gmLightboxNext {
                font-size: 18px;
                padding: 8px 10px;
            }
        }

        @media (max-width: 420px) {
            .cleanSpoilerGallery .spoilerGalleryItem {
                width: 100%;
            }

            .cleanSpoilerGallery .spoilerGalleryItem img {
                height: 150px;
            }
        }
    </style>
    
    <div class="gm-actions"><a class="gm-btn gm-btn-download" href="https://www.gamemaps.com/download/eyJpZHMiOlszNTgyMl19?s=21d3b7ad12bfe339d480b486f776cf7e5bc35313d0d438d75acb91e4bebcbe8c&amp;e=1778471271" target="_blank" rel="nofollow noopener noreferrer">Direct Download</a></div>
    
    <div class="gm-media-label">Media</div>
    <div class="ipsSpoiler cleanSpoilerGallery" data-ipsspoiler>
        <div class="ipsSpoiler_header">
            <span>Media gallery</span>
        </div>
        <div class="ipsSpoiler_contents ipsClearfix">
            <div class="spoilerGalleryGrid">
                
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_0.jpg?1778101722"
               data-gm-gallery="gm-gallery-35822"
               data-gm-index="0"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35822"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_0.jpg?1778101722" alt="GameMaps media 1" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_1.jpg?1778101722"
               data-gm-gallery="gm-gallery-35822"
               data-gm-index="1"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35822"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_1.jpg?1778101722" alt="GameMaps media 2" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_2.jpg?1778101722"
               data-gm-gallery="gm-gallery-35822"
               data-gm-index="2"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35822"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_2.jpg?1778101722" alt="GameMaps media 3" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_3.jpg?1778101722"
               data-gm-gallery="gm-gallery-35822"
               data-gm-index="3"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35822"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_3.jpg?1778101722" alt="GameMaps media 4" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_4.jpg?1778101722"
               data-gm-gallery="gm-gallery-35822"
               data-gm-index="4"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35822"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_4.jpg?1778101722" alt="GameMaps media 5" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_5.jpg?1778101722"
               data-gm-gallery="gm-gallery-35822"
               data-gm-index="5"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35822"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_5.jpg?1778101722" alt="GameMaps media 6" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_6.jpg?1778101722"
               data-gm-gallery="gm-gallery-35822"
               data-gm-index="6"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35822"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_6.jpg?1778101722" alt="GameMaps media 7" loading="lazy">
            </a>
            
            <a class="spoilerGalleryItem"
               href="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_7.jpg?1778101722"
               data-gm-gallery="gm-gallery-35822"
               data-gm-index="7"
               data-ipslightbox
               data-ipslightbox-group="gm-gallery-35822"
               target="_blank"
               rel="noopener noreferrer">
                <img src="https://www.gamemaps.com/img/addons/l4d2/ss/only_comm_ultimis_edward_richtofen__request__35822_7.jpg?1778101722" alt="GameMaps media 8" loading="lazy">
            </a>
            
            </div>
        </div>
    </div>

    <div class="gmLightbox" data-gm-lightbox="gm-gallery-35822" hidden>
        <button type="button" class="gmLightboxBtn gmLightboxClose" aria-label="Close">&times;</button>
        <button type="button" class="gmLightboxBtn gmLightboxPrev" aria-label="Previous image">&#10094;</button>
        <img class="gmLightboxImage" src="" alt="">
        <button type="button" class="gmLightboxBtn gmLightboxNext" aria-label="Next image">&#10095;</button>
        <div class="gmLightboxCounter">1 / 8</div>
    </div>

    
    <script>
    (function () {
        if (window.__gmGalleryInitialized) {
            return;
        }
        window.__gmGalleryInitialized = true;

        function getOpenModal() {
            return document.querySelector('.gmLightbox.is-open');
        }

        function getGalleryLinks(group) {
            var allLinks = Array.prototype.slice.call(document.querySelectorAll('a[data-gm-gallery]'));
            return allLinks.filter(function (link) {
                return link.getAttribute('data-gm-gallery') === group;
            });
        }

        function closeModal(modal) {
            if (!modal) {
                return;
            }
            modal.classList.remove('is-open');
            modal.setAttribute('hidden', 'hidden');
            var image = modal.querySelector('.gmLightboxImage');
            if (image) {
                image.removeAttribute('src');
            }
            document.documentElement.classList.remove('gmLightboxOpen');
        }

        function renderModal(modal) {
            var links = modal.__gmLinks || [];
            if (!links.length) {
                return;
            }

            var index = modal.__gmIndex || 0;
            if (index < 0) {
                index = links.length - 1;
            }
            if (index >= links.length) {
                index = 0;
            }

            modal.__gmIndex = index;
            var activeLink = links[index];
            var image = modal.querySelector('.gmLightboxImage');
            var thumb = activeLink.querySelector('img');
            var counter = modal.querySelector('.gmLightboxCounter');

            if (image) {
                image.setAttribute('src', activeLink.getAttribute('href'));
                image.setAttribute('alt', thumb ? (thumb.getAttribute('alt') || '') : '');
            }

            if (counter) {
                counter.textContent = (index + 1) + ' / ' + links.length;
            }
        }

        function openModal(modal, links, startIndex) {
            if (!modal || !links.length) {
                return;
            }
            modal.__gmLinks = links;
            modal.__gmIndex = startIndex;
            renderModal(modal);
            modal.removeAttribute('hidden');
            modal.classList.add('is-open');
            document.documentElement.classList.add('gmLightboxOpen');
        }

        function stepModal(modal, delta) {
            if (!modal) {
                return;
            }
            modal.__gmIndex = (modal.__gmIndex || 0) + delta;
            renderModal(modal);
        }

        document.addEventListener('click', function (event) {
            var galleryItem = event.target.closest('a[data-gm-gallery]');
            if (galleryItem) {
                var group = galleryItem.getAttribute('data-gm-gallery');
                var modal = document.querySelector('.gmLightbox[data-gm-lightbox="' + group + '"]');
                var links = getGalleryLinks(group);
                var startIndex = parseInt(galleryItem.getAttribute('data-gm-index') || '0', 10);
                if (modal && links.length) {
                    event.preventDefault();
                    openModal(modal, links, startIndex);
                }
                return;
            }

            var closeButton = event.target.closest('.gmLightboxClose');
            if (closeButton) {
                closeModal(closeButton.closest('.gmLightbox'));
                return;
            }

            var prevButton = event.target.closest('.gmLightboxPrev');
            if (prevButton) {
                stepModal(prevButton.closest('.gmLightbox'), -1);
                return;
            }

            var nextButton = event.target.closest('.gmLightboxNext');
            if (nextButton) {
                stepModal(nextButton.closest('.gmLightbox'), 1);
                return;
            }

            var openModalElement = event.target.classList.contains('gmLightbox') ? event.target : null;
            if (openModalElement) {
                closeModal(openModalElement);
            }
        });

        document.addEventListener('keydown', function (event) {
            var modal = getOpenModal();
            if (!modal) {
                return;
            }

            if (event.key === 'Escape') {
                closeModal(modal);
            } else if (event.key === 'ArrowLeft') {
                stepModal(modal, -1);
            } else if (event.key === 'ArrowRight') {
                stepModal(modal, 1);
            }
        });
    })();
    </script>
    
    
    <div class="gm-description-text">Only COMM Ultimis Edward Richtofen (request)

A character from the **** Zombies series, a bonus mode of the Call of Duty series. - Edward Richtofen (Ultimis) survivor Skin.
Replaces all survivors L4D1(bill,francis,louis,zoey),L4D2 (nick,coach,ellis,rochelle) with only zoey

Credits:
skin - EnergyFlamer [COMM OPEN] [COMM: Ultimis Edward Richtofen [Francis]]
skin,sound edit - l4dkk

9.vpk files are included in the compressed file.

COMM Ultimis Edward Richtofen bill.vpk (bill skin)
COMM Ultimis Edward Richtofen francis.vpk (francis skin)
COMM Ultimis Edward Richtofen louis.vpk (Louis skin)
COMM Ultimis Edward Richtofen zoey.vpk (zoey skin)
COMM Ultimis Edward Richtofen coach.vpk (coach skin)
COMM Ultimis Edward Richtofen elliss.vpk (ellis skin)
COMM Ultimis Edward Richtofen nick.vpk (nick skin)
COMM Ultimis Edward Richtofen rochelle.vpk (Rochelle skin)
Only COMM Ultimis Edward Richtofen.vpk (all survivors)

This skin works best on local servers and singleplayer.</div>
    ]]></content:encoded>
      <pubDate>Wed, 06 May 2026 21:12:00 +0000</pubDate>
    </item>
  </channel>
</rss>
